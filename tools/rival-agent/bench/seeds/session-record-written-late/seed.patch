diff --git a/tools/rival-agent/launch.mjs b/tools/rival-agent/launch.mjs
index 1aa16d32c..1d36d0fe7 100644
--- a/tools/rival-agent/launch.mjs
+++ b/tools/rival-agent/launch.mjs
@@ -208,8 +208,6 @@ export async function launch(
   onProgress(`session: ${session}`);
   for (const note of notes) onProgress(note);
 
-  // session.json lands before provisioning so a failure there is still observable through
-  // `broker next` rather than as "no session".
   const writeSessionRecord = () =>
     writeJsonAtomic(sessionPath(session, SESSION_FILES.session), {
       id,
@@ -224,7 +222,6 @@ export async function launch(
       branch,
       createdAt: new Date().toISOString(),
     });
-  writeSessionRecord();
   let attempt = 0;
   let rivalSession;
 

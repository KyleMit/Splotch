diff --git a/tools/rival-agent/README.md b/tools/rival-agent/README.md
index 3b4210fe3..7641284b8 100644
--- a/tools/rival-agent/README.md
+++ b/tools/rival-agent/README.md
@@ -10,8 +10,8 @@ history: why this shape, what was rejected, the accepted exposures, and the Clau
 history: why this shape, what was rejected, the accepted exposures, and the Claude versus Codex
 parity table.
 
-The runtime files in this folder import nothing from outside it. The Codex-side installer copies
-those files into `~/.local/libexec` as hashed trusted bytes, where the rest of the checkout does not
+The runtime files in this folder import nothing from outside it, because the Codex-side installer
+copies them into `~/.local/libexec` as hashed trusted bytes, where the rest of the checkout does not
 exist. The checkout-only live acceptance generator and its templates are not installed.
 
 ## Entry points

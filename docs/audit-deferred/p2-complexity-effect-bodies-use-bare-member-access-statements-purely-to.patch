diff --git a/eslint.config.js b/eslint.config.js
index 745f67f..f4de1cd 100644
--- a/eslint.config.js
+++ b/eslint.config.js
@@ -50,9 +50,6 @@ export default tseslint.config(
       parserOptions: { parser: tseslint.parser },
     },
     rules: {
-      // Bare member reads inside $effect register reactive dependencies (idiomatic Svelte 5);
-      // they are intentional, not dead expressions.
-      '@typescript-eslint/no-unused-expressions': 'off',
       // Misfires on `$bindable()` destructuring defaults, which read as unused assignments.
       'no-useless-assignment': 'off',
     },
diff --git a/web/src/lib/actions/pinchTextZoom.svelte.ts b/web/src/lib/actions/pinchTextZoom.svelte.ts
index 5b2add1..c358260 100644
--- a/web/src/lib/actions/pinchTextZoom.svelte.ts
+++ b/web/src/lib/actions/pinchTextZoom.svelte.ts
@@ -64,7 +64,16 @@ export function pinchTextZoom(node: HTMLElement, getOptions: () => PinchTextZoom
     if (target) target.style.zoom = zoom === MIN_TEXT_ZOOM ? '' : String(zoom);
   }
 
-  function reset() {
+  // Keyed on the two options that are meant to trigger a reset, so the $effect
+  // below hands them over instead of touching them purely to be observed by the
+  // dependency tracker — drop one and this call stops compiling. The dedupe also
+  // keeps an unrelated option change (`target`, which lands after mount via
+  // bind:this) from shrinking text the reader has pinched larger.
+  let lastReset: { enabled: boolean; resetKey: unknown } | null = null;
+
+  function reset(enabled: boolean, resetKey: unknown) {
+    if (lastReset && lastReset.enabled === enabled && lastReset.resetKey === resetKey) return;
+    lastReset = { enabled, resetKey };
     points.clear();
     zoom = MIN_TEXT_ZOOM;
     baseZoom = MIN_TEXT_ZOOM;
@@ -130,12 +139,12 @@ export function pinchTextZoom(node: HTMLElement, getOptions: () => PinchTextZoom
   node.addEventListener('click', onClickCapture, true);
 
   // Reset to normal size whenever the gate toggles or the overlay reopens.
-  // Reading these runes here is what subscribes the action to them.
+  // Calling the getter is what subscribes the action to every rune it reads;
+  // passing the two trigger options into reset is what makes that subscription
+  // load-bearing.
   $effect(() => {
     const o = getOptions();
-    void o.enabled;
-    void o.resetKey;
-    reset();
+    reset(o.enabled, o.resetKey);
   });
 
   return {
diff --git a/web/src/lib/actions/pinchZoom.svelte.ts b/web/src/lib/actions/pinchZoom.svelte.ts
index 623f6c8..bdbcb2a 100644
--- a/web/src/lib/actions/pinchZoom.svelte.ts
+++ b/web/src/lib/actions/pinchZoom.svelte.ts
@@ -184,7 +184,16 @@ export function pinchZoom(node: HTMLElement, getOptions: () => PinchZoomOptions)
     node.classList.toggle('zoomed', zoom.isZoomed);
   }
 
-  function reset(target: HTMLElement | undefined) {
+  // Keyed on the two options that are meant to trigger a reset, so the $effect
+  // below hands them over instead of touching them purely to be observed by the
+  // dependency tracker — drop one and this call stops compiling. The dedupe also
+  // keeps an unrelated option change (`target`, which lands after mount via
+  // bind:this) from wiping a zoom the user is holding.
+  let lastReset: { enabled: boolean; resetKey: unknown } | null = null;
+
+  function reset(target: HTMLElement | undefined, enabled: boolean, resetKey: unknown) {
+    if (lastReset && lastReset.enabled === enabled && lastReset.resetKey === resetKey) return;
+    lastReset = { enabled, resetKey };
     zoom.reset();
     if (target) target.style.transform = '';
     node.classList.remove('zoomed');
@@ -232,14 +241,12 @@ export function pinchZoom(node: HTMLElement, getOptions: () => PinchZoomOptions)
   node.addEventListener('pointerup', onPointerUp);
   node.addEventListener('pointercancel', onPointerUp);
 
-  // Reset to fit whenever the gate toggles or a new image arrives. Reading these
-  // runes here is what subscribes the action to them; each run returns the
-  // preview to its un-zoomed, centered state.
+  // Reset to fit whenever the gate toggles or a new image arrives. Calling the
+  // getter is what subscribes the action to every rune it reads; passing the two
+  // trigger options into reset is what makes that subscription load-bearing.
   $effect(() => {
     const o = getOptions();
-    void o.enabled;
-    void o.resetKey;
-    reset(o.target);
+    reset(o.target, o.enabled, o.resetKey);
   });
 
   return {
diff --git a/web/src/lib/boot/persistedState.ts b/web/src/lib/boot/persistedState.ts
index ec894f5..7a2ce8d 100644
--- a/web/src/lib/boot/persistedState.ts
+++ b/web/src/lib/boot/persistedState.ts
@@ -2,6 +2,7 @@ import { hydrateApiKey } from '$lib/state/aiKey.svelte';
 import { hydrateSaveFolder } from '$lib/state/saveFolder.svelte';
 import { hydrateDurableStorage } from '$lib/storage';
 import { applyDeviceOrientationPreference } from '$lib/orientation';
+import { settings } from '$lib/state/settings.svelte';
 
 export function hydratePersistedState(): void {
   // Load the BYOK Gemini key from secure storage into the live store (async,
@@ -21,6 +22,11 @@ export function hydratePersistedState(): void {
   // but this guarantees the apply even when the restored value equals the
   // current one.
   hydrateDurableStorage().then((restored) => {
-    if (restored) applyDeviceOrientationPreference();
+    if (restored) {
+      applyDeviceOrientationPreference(
+        settings.lockRotationEnabled,
+        settings.forceLandscapeOrientation
+      );
+    }
   });
 }
diff --git a/web/src/lib/components/ClearButton.svelte b/web/src/lib/components/ClearButton.svelte
index 8735c2f..f0a160e 100644
--- a/web/src/lib/components/ClearButton.svelte
+++ b/web/src/lib/components/ClearButton.svelte
@@ -5,7 +5,7 @@
   import { clearCanvas } from '$lib/drawing/engine';
   import { saveDrawingIfEnabled } from '$lib/drawing/saveOnDelete';
   import { dragToClear } from '$lib/actions/dragToClear';
-  import { layout } from '$lib/state/layout.svelte';
+  import { layout, type Orientation } from '$lib/state/layout.svelte';
   import { resetToolAfterClear } from '$lib/state/tool.svelte';
 
   let containerEl: HTMLDivElement;
@@ -18,9 +18,15 @@
   // Tracked so resetButtonPosition can skip a reset mid-gesture.
   let isDragging = false;
 
-  function resetButtonPosition() {
+  let lastResetOrientation: Orientation | null = null;
+
+  function resetButtonPosition(orientation: Orientation) {
+    if (orientation === lastResetOrientation) return;
     coachmark?.dismiss(); // geometry would be stale after a layout change
     if (!containerEl || isDragging) return;
+    // Recorded only once the reset actually happened, so a rotation skipped
+    // mid-gesture is still pending if the orientation flips back to it.
+    lastResetOrientation = orientation;
     containerEl.style.transform = '';
   }
 
@@ -28,10 +34,13 @@
   // plain same-orientation resizes leave a mid-drag or settled position alone.
   // untrack the reset so this fires only on an orientation change — the dismiss
   // it calls reads the coachmark's visibility state, and subscribing to that
-  // would re-run this effect on reveal and instantly dismiss the tutorial.
+  // would re-run this effect on reveal and instantly dismiss the tutorial. The
+  // orientation is threaded in as an argument (and deduped against the last
+  // reset) so the dependency is load-bearing: drop it and the call stops
+  // compiling, rather than silently losing its only trigger.
   $effect(() => {
-    layout.orientation;
-    untrack(resetButtonPosition);
+    const orientation = layout.orientation;
+    untrack(() => resetButtonPosition(orientation));
   });
 </script>
 
diff --git a/web/src/lib/orientation.ts b/web/src/lib/orientation.ts
index 7d7cd47..8f6f6e0 100644
--- a/web/src/lib/orientation.ts
+++ b/web/src/lib/orientation.ts
@@ -1,6 +1,5 @@
 import { browser } from '$app/environment';
 import { isNative, supportsOrientationLock } from '$lib/platform';
-import { settings } from '$lib/state/settings.svelte';
 
 type OrientationLockType = 'portrait' | 'landscape';
 
@@ -11,15 +10,18 @@ type LockableScreenOrientation = ScreenOrientation & {
 
 let lastRequested: OrientationLockType | 'unlocked' | null = null;
 
-export async function applyDeviceOrientationPreference() {
+export async function applyDeviceOrientationPreference(
+  lockRotationEnabled: boolean,
+  forceLandscapeOrientation: boolean
+) {
   if (!browser) return;
 
   // Windowed platforms (iPadOS 26+) own orientation themselves; locking there
   // only floats a letterboxed window, so leave it to the OS window controls.
   if (!supportsOrientationLock()) return;
 
-  const target: OrientationLockType | 'unlocked' = settings.lockRotationEnabled
-    ? settings.forceLandscapeOrientation
+  const target: OrientationLockType | 'unlocked' = lockRotationEnabled
+    ? forceLandscapeOrientation
       ? 'landscape'
       : 'portrait'
     : 'unlocked';
diff --git a/web/src/routes/+page.svelte b/web/src/routes/+page.svelte
index ab84020..fed8b07 100644
--- a/web/src/routes/+page.svelte
+++ b/web/src/routes/+page.svelte
@@ -25,9 +25,10 @@
   import { initWebOnlyServices } from '$lib/boot/webOnlyServices';
 
   $effect(() => {
-    settings.lockRotationEnabled;
-    settings.forceLandscapeOrientation;
-    applyDeviceOrientationPreference();
+    applyDeviceOrientationPreference(
+      settings.lockRotationEnabled,
+      settings.forceLandscapeOrientation
+    );
   });
 
   // Own the drawing route's app-surface locks (ADR-0076): no scroll, selection,

# Splotch – Architecture

## Tech Stack

### Core Framework

* **[SvelteKit](https://kit.svelte.dev/)** — full-stack framework. Web target uses `adapter-netlify`
  (SSR + serverless functions). Native target uses `adapter-static` (fully static export bundled via
  Capacitor).
* **[Svelte 5](https://svelte.dev/)** — UI components with runes (`$state`, `$effect`, `$derived`)
  for reactivity. No legacy stores.
* **[TypeScript](https://www.typescriptlang.org/)** — strict typing across the whole codebase.
* **[Vite](https://vite.dev/)** — build tool. Compile-time constants are declared centrally in
  `web/defines.ts` and injected by `web/vite.config.ts`.

### Native (Capacitor)

* **[Capacitor 8](https://capacitorjs.com/)** — wraps the static web export in an Android/iOS shell.
  The native build is triggered by `CAPACITOR=true` at build time; `web/svelte.config.js` and
  `web/vite.config.ts` both branch on this env var.
* **`@capacitor/network`** — detects online/offline state to show or hide the AI button on device.
* **`@capacitor/preferences`** — durable storage (UserDefaults/SharedPreferences) that backs up
  settings the WebView's localStorage might evict.
* **`@capacitor-community/media`** — saves drawings to the device photo library in a "Splotch"
  album.
* **`@aparajita/capacitor-secure-storage`** — stores client-held AI credentials securely on-device.

### AI

* **[`openai`](https://platform.openai.com/)** — OpenAI API client. Image generation runs
  server-side (Netlify function `/api/generate-image`) and supports a server-authoritative free
  installation grant, managed access tokens, or BYOK. Native apps call the hosted endpoint via
  `__NATIVE_API_BASE__`. The SDK is confined to the OpenAI adapter behind the provider-agnostic
  `AiImageProvider` seam in `lib/server/ai/` (ADR-0047) — routes never import it.
* **`@google/genai`** — dev-only. The app no longer calls Gemini; the SDK stays a `devDependency`
  for the asset-generation pipeline in `tools/asset-gen/` and the Gemini baseline rows of the
  image-model bake-off.

### Build & PWA

* **[vite-plugin-pwa](https://vite-pwa-org.netlify.app/)** — service worker and offline support.
  Web-only; skipped entirely when `CAPACITOR=true` (the native shell provides equivalent offline
  capability).
* **[`@fontsource-variable/quicksand`](https://fontsource.org/fonts/quicksand)** — self-hosted
  variable font, bundled for offline use.

### Testing

* **[Vitest](https://vitest.dev/) + happy-dom** — unit tests for pure logic and state modules
  (`npm run test:unit`).
* **[Playwright](https://playwright.dev/)** — E2E web tests against the production build
  (`npm run test:e2e`).
* **[Maestro](https://maestro.mobile.dev/)** — Android and iOS smoke tests that boot a real emulator
  or simulator and assert the UI renders (`npm run test:android`, `npm run test:ios`). See the
  `testing` skill.

---

## Source Map

### `web/src/lib/`

| Path                                 | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `drawing/aiImage.ts`                 | Client AI-generation pipeline: snapshots the drawing, chooses the upload encoding, calls `/api/generate-image`, coordinates the result state (including the closed style used by reporting), and optionally saves the drawing and generated image.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `drawing/aiImageResponse.ts`         | Classifies image-generation responses into image, safety refusal, throttling, or server error outcomes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `drawing/coloringAppearance.ts`      | Bridges coloring-page selection to drawing history (ADR-0121): applies or removes the selected page, opens a magic-recode undo unit only when retained magic ink exists, and restores the prior page/orientation when that unit is undone.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `drawing/engine.ts`                  | Imperative drawing facade and input orchestrator (ADR-0004/0085). Owns the transparent input canvas, paper coordinate space, pointer tracking (including the WebKit pen quirks and edge-swipe guards), brush state, synchronous export snapshots, and the public API. Components connect via callbacks (`onDrawSound`, `onUndoStateChange`, etc.) and direct calls (`setColor`, `setStrokeWidth`, `clearCanvas`).                                                                                                                                                                                                                                                                                   |
| `drawing/engineListeners.ts`         | Registers the engine's window/document/canvas listeners through the teardown registry, including delayed orientation-angle resync, background re-entry, WebKit pen-stream adoption, and the non-passive iPadOS Scribble touch guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `drawing/earlyBoot.ts`               | Pre-hydration drawing-engine startup (ADR-0072) and the shared bridge that pushes the restored brush and stroke width into the engine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `drawing/perf.ts`                    | Compile-time-gated drawing hot-path performance marks consumed by the profiling harness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `drawing/canvasMeasure.ts`           | The canvas's measured geometry: the cached client rect and backing-pixel scale the pointer hot path maps through, plus the rule that a client rect with no area is not a layout but the absence of one — refused rather than adopted, with a one-shot `ResizeObserver` that re-runs the skipped rebuild on the first layout that gives the canvas a box. Adopting an unmeasured rect collapses the paper and every live tile to zero area, leaving a canvas that silently accepts strokes and paints nothing.                                                                                                                                                                                       |
| `drawing/canvasContextRecovery.ts`   | Progressive Canvas 2D context-loss recovery for disposable worker surfaces (ADR-0110): observes loss/restoration where supported, probes context state, replays a complete idempotent paint once while source inputs remain worker-owned, and rejects repeated loss with a stable code. It does not recover the live tiled drawing surface.                                                                                                                                                                                                                                                                                                                                                         |
| `drawing/tiledRenderer.ts`           | Production drawing renderer and history orchestrator (ADR-0085): culls operations across the live tiles, keeps the undoable vector tail, idle-folds older commands into tile-sized raster bases, and exposes tiled export snapshots without mutating a full-screen canvas during input.                                                                                                                                                                                                                                                                                                                                                                                                             |
| `drawing/tiledMagicRecode.ts`        | Magic recoloring history (ADR-0121): retains the pre-magic raster checkpoint plus folded vector tail, rebinds retained magic ops to a published page/theme sheet, and restores prior sheet references for page undo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `drawing/tiledRendererReadback.ts`   | Read-only tiled renderer projections: history/debug accounting, visible-tile bitmap capture, and full drawing replay for export without owning mutable renderer state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `drawing/tiledSurfaces.ts`           | Adopts and manages the shared component's live tile canvases, lazy crayon backing planes, raster history-base tiles, and tile-local paper-view transforms; owns their clear, resize, and tile-local history rendering mechanics. Missing or incomplete template surfaces are an initialization error.                                                                                                                                                                                                                                                                                                                                                                                               |
| `drawing/liveTiles.ts`               | The shared 4×4 production tile-grid dimensions and count (ADR-0085), imported by the component, renderer, and tests so surface topology cannot drift.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `drawing/liveTileComposite.ts`       | Reconstructs the visible normal live-tile plane as one backing-pixel canvas for `/dev/engine` diagnostics and production-route E2E assertions. It derives row and column offsets from each tile's backing dimensions, so fractional-DPR CSS snapping cannot move pixels across tile boundaries.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `drawing/listenerRegistry.ts`        | Typed registration and teardown for the drawing engine's pointer, touch, and context-menu listeners.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `drawing/strokeOps.ts`               | The engine's op vocabulary (`StrokeOp`, `StrokeGroupCommand`) and the single `renderOp()` renderer used by the live tiles, history replay, compaction, and export so every production surface is bit-identical (ADR-0085).                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `drawing/crayonPassBuffer.ts`        | Per-target crayon accumulation and two-blit subtractive glaze stamping (ADR-0065/0085). Every live tile and export target has its own bounded pass buffer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `drawing/opGeometry.ts`              | An op's bare geometry, shared by the renderer and the pass buffer without either importing the other: its user-space extent, the `AA_PAD_PX` bleed convention, and the painter that lays a dot/path shape down with a caller-supplied paint.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `drawing/crayonBrush.ts`             | The crayon brush's paint source (ADR-0065): a deterministic tileable paper-tooth field and per-(colour,pass) repeating `CanvasPattern`, phase-shifted by a stored per-stroke seed so overlapping same-colour strokes build up (fill tooth) at a constant hue. Owns the tunable knobs and dev A/B seam.                                                                                                                                                                                                                                                                                                                                                                                              |
| `drawing/magicBrush.ts`              | Magic-brush color-sheet source (ADR-0043): rasterizes coloring-page fills or generated rainbows into the paper coordinate space and supplies the patterns revealed by magic strokes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `drawing/undoHistory.ts`             | Shared undo contracts: the product's maximum depth and the debug shape implemented by `tiledRenderer.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `drawing/emptyScan.ts`               | Cheap blank-canvas detection on a small CPU-side scratch canvas (keeps `willReadFrequently` off the main canvas).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `drawing/exportDrawing.ts`           | Composes the shareable PNG (paper white + texture + the stroke snapshot the engine passes in + coloring-page overlay) and owns the paper-texture loader. Save-time-only: `engine.exportCanvasBlob` loads it on demand after synchronously snapshotting the strokes, so it stays out of the startup bundle — `web/tests/startup-bundle.spec.ts` guards it (with `screenshot.ts`, `folderSave.ts`, and `polaroidAnimation.ts`) off the modulepreload list.                                                                                                                                                                                                                                            |
| `drawing/exportCompositor.ts`        | Shared paper, texture, smoothing, and contain-fit coloring-overlay composition used by both the main-thread export fallback and the PNG worker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `drawing/pngEncoder.ts`              | Main-thread PNG-export coordinator: sends whole-canvas or tiled snapshots to the worker, bounds worker requests with a timeout, and preserves a synchronous/main-thread fallback when worker APIs fail or are unavailable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `drawing/tiledPngCompositor.ts`      | Reassembles transferred tile bitmaps into a full PNG inside the worker using the same export compositor as the main-thread path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `drawing/strokeMath.ts`              | Pure gesture math (edge-swipe guards, pointer-resume detection, stroke speed) factored out of the engine for unit testing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `drawing/paperView.ts`               | Pure paper-view geometry (ADR-0050): the upright contain-fit + center transform (matrix/inverse forms) that presents the rotation-locked "paper" — the space ops are recorded in — inside a rotated viewport. The engine owns the paper state and applies the view; `DrawingCanvas.svelte` reuses `viewMatrix` to position the overlay wrapper.                                                                                                                                                                                                                                                                                                                                                     |
| `drawing/overlay.ts`                 | Manages the coloring-book overlay image rendered behind the drawing layer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `drawing/saveOnDelete.ts`            | Saves the current drawing to the gallery before clearing, when the setting is enabled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `components/LiveSurface.svelte`      | The drawing engine's single DOM contract: transparent `#drawingCanvas` pointer receiver, isolated `.canvas-stack`, CSS-presented `.live-paper-view`, and the fixed normal/bottom-crayon/top-crayon tile planes shared by `/` and `/dev/engine`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `drawing/polaroidAnimation.ts`       | Builds and tears down the screenshot polaroid DOM feedback, sizing the photo from the active canvas and flying it from the Screenshot Button. Loaded only through the save pipeline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `drawing/screenshot.ts`              | Persists canvas PNGs (exported via `engine.exportCanvasBlob`): on native, saves to the photo library; on web, writes to the chosen folder or triggers a download. `saveScreenshot` starts `polaroidAnimation.ts` before persistence so a folder-permission prompt cannot delay feedback.                                                                                                                                                                                                                                                                                                                                                                                                            |
| `drawing/folderSave.ts`              | Web-only File System Access integration for choosing, persisting, and writing silently to an optional save folder; IndexedDB holds the structured-clone directory handle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `saveNaming.ts`                      | Shared MIME-aware PNG/WebP/JPEG naming, drawing/AI basenames, and browser-download anchor helper. Kept separate from `drawing/screenshot.ts` so the AI result's Download button and AI auto-save can reuse naming without statically loading the save pipeline.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `state/aiGeneration.svelte.ts`       | Reactive AI-result state plus the latest-run state machine that owns request cancellation, object-URL cleanup, and stale-result rejection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `state/aiProgress.svelte.ts`         | How far along the running generation is and whether its picture has been revealed, followed off the generation machine rather than owned by a component (ADR-0117). It holds the rAF loop above both surfaces that show a run — the result modal's dial and the Waiting Polaroid's caption bar — so minimizing neither pauses a run nor restarts it, and a picture that lands while minimized is revealed outright instead of ramping behind a dial nobody is watching.                                                                                                                                                                                                                             |
| `state/freeGenerations.svelte.ts`    | Client view of the server-authoritative ten-generation installation grant, including the app-scoped installation pseudonym and remaining-count refresh.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `state/aiKey.ts`                     | BYO API-key persistence and boot hydration through the shared secure-credential coordinator, including migration from the retired plaintext storage key into secure storage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `state/aiAccessToken.ts`             | Managed access-code persistence and boot hydration through the shared secure-credential coordinator, plus URL-invitation capture and migration from the retired plaintext storage key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `state/canvas.svelte.ts`             | Thin `$state` object bridging the imperative engine's callbacks (`canUndo`, `canvasEmpty`) into Svelte reactivity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `state/colors.svelte.ts`             | Active color selection and the full palette.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `state/strokeWidth.svelte.ts`        | Stroke width levels and eraser size multiplier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `state/tool.svelte.ts`               | The active brush — one four-way axis (`pen \| crayon \| magic \| eraser`, ADR-0067), persisted as `splotch-brush-type` (default pen; the eraser is never persisted). Pen and crayon are the "ink brushes" that lay the active palette color; `selectInkBrush()` returns to the last one when a color pick or clear leaves the eraser/magic.                                                                                                                                                                                                                                                                                                                                                         |
| `state/settings.svelte.ts`           | User-configurable toggles (sounds, save-on-delete, screenshot button, coloring books, each optional brush, etc.) plus the appearance setting (light/dark/system), persisted via `storage.ts`. Disabling an optional brush normalizes the active tool and remembered ink fallback so hidden tools cannot return indirectly.                                                                                                                                                                                                                                                                                                                                                                          |
| `state/sectionsSeen.svelte.ts`       | Per-section seen stamps for Settings activity dots. A missing or stale stamp marks a section as new; opening or reading it persists the current stamp through `storage.ts` from the first session, while presentation stays gated by the Settings counter in `sessionCounters.svelte.ts`.                                                                                                                                                                                                                                                                                                                                                                                                           |
| `state/sessionCounters.svelte.ts`    | Shared saturating, persisted counters that record each `Document` at most once per purpose. Settings records every app session and reveals activity dots on session six; the install flow records only sessions that reach the settled-in stroke threshold while a re-prompt is pending. Purpose-specific keys keep each count's narrow use legible in storage.                                                                                                                                                                                                                                                                                                                                     |
| `state/parentalGate.svelte.ts`       | The Grown-Ups Only parental gate (App Store 5.1.4, ADR-0094): the multiplication challenge, Parent Center's independent `always`/`session`/`never` policies, and `requireParentalGate()`. Gates sit at operation boundaries — AI image launch, AI result reporting, external-link activation, feedback submission, and Parent Center entry. Policies persist via `storage.ts`; per-session solves stay in memory.                                                                                                                                                                                                                                                                                   |
| `state/appearance.svelte.ts`         | Reactive *resolved* theme ('light' \| 'dark'): the appearance setting combined with the live OS preference. For the few JS consumers of the resolved value (Notch Band paper color, export paper fill); CSS reads the `app.css` tokens instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `state/layout.svelte.ts`             | Viewport and orientation state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `state/fullscreen.svelte.ts`         | Immersive-fullscreen support + active state and the toggle action, backing the Fullscreen Toggle button. Android web only; dismisses the mobile URL bar.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `state/network.svelte.ts`            | Online/offline state via `@capacitor/network`. Controls AI button visibility on native.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `state/install.svelte.ts`            | PWA install state (ADR-0039). Captures Chromium's `beforeinstallprompt` for one-tap install, falls back to iOS/Android guided hints, and owns the bounded 5/10-qualified-session re-prompt policy; drives the Install Banner and the Settings Install section. Web-only; inert in the native shell.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `state/coloringBook.svelte.ts`       | Selected coloring book and page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `state/books.ts`                     | Static catalog of available coloring books and pages, the Farm starter id, each book's exact 73-file logical runtime inventory plus compact derivative mapping, and the canonical/native-local URL resolution boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `state/coloringPacks.svelte.ts`      | Reactive installed-book availability and background-download/storage status. Starts with Farm only; the picker observes it so partial packs never appear.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `state/modal.svelte.ts`              | Reusable reactive modal state, including the triggering control's screen-space origin for anchored open animations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `state/saveFolder.svelte.ts`         | Startup-safe reactive mirror and lazy UI actions for the optional web save folder, keeping the save pipeline out of the preload graph.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `state/ui.svelte.ts`                 | Cross-component UI state, modal instances, and shared control ids that must stay on the startup path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `actions/dragToClear.ts`             | Svelte action that implements the drag-to-clear gesture (pointer tracking, threshold detection, animations). Keeps all gesture logic out of the component.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `actions/launchGuard.ts`             | Short-lived tap dead zones that prevent toddler tap bursts from immediately dismissing a newly opened modal or activating content revealed under the first tap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `actions/modalDialog.svelte.ts`      | Svelte action wrapping the native `<dialog>` element with open/close state management.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `actions/pinchTextZoom.svelte.ts`    | Reflowing two-finger text zoom for scrollable panes, with native one-finger scrolling and ghost-click suppression preserved.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `actions/pinchZoom.svelte.ts`        | Bounded pinch-to-zoom and pan for the fixed-size AI image preview.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `actions/pointerCapture.ts`          | Exception-safe pointer capture/release helpers shared by the pinch actions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `actions/scribbleGuard.ts`           | iPadOS Scribble suppression and pen-tap activation for controls a stylus uses immediately before returning to the canvas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `actions/scrollCue.ts`               | Scroll affordances for a grid that outgrows its dialog: budgets the scrollport so the fold cuts a tile row in half, reports when the end of the content is reached so a bottom fade can retire, and publishes the scrollport's bottom padding so that fade reaches the edge the scrollport clips at rather than stopping one padding above it.                                                                                                                                                                                                                                                                                                                                                      |
| `actions/spreadTracker.ts`           | DOM-free multi-pointer position and spread bookkeeping shared by the two pinch gestures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `coloringPacks/`                     | ADR-0103's versioned manifest/store contract, native URL resolver, web Cache Storage implementation, native Capacitor facade, and sequential idle/background download manager. The manager and platform store are dynamically imported off the startup/hot path; the Coloring Book setting gates manifest loading and cancels active web/native work without deleting completed packs.                                                                                                                                                                                                                                                                                                              |
| `plugins/coloringPacks.ts`           | Typed Capacitor bridge to the app-local Android WorkManager and iOS background-URLSession installers. Native-only dynamic dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `boot/`                              | The drawing route's named boot steps, called in order from `routes/+page.svelte`'s `onMount`: `hydratePersistedState()` (AI credentials, save folder, durable-storage recovery), then `mountBootHiddenOverlays()` (the idle overlay pump, ADR-0049), `installContextMenuGuard()`, `installWakeLock()`, `initWebOnlyServices()` (PWA updates + install prompt, web-only), `installColoringPackDownloads()` (durable-settings-aware gated idle import; ADR-0103), and `installUndoShortcut()` (window-level Ctrl/Cmd+Z). They return teardowns the route runs on unmount — page-lifecycle-scoped imperative wiring, as distinct from self-initializing stores in `state/`.                            |
| `components/`                        | Svelte UI components. Each component owns its scoped styles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `components/AiImageReport.svelte`    | Confirmed AI-result and false-positive-refusal reporting UI and client upload workflow: the report-specific disclosure dialog, multipart evidence send, and success/error states. The order is Report → parental gate → confirm dialog → send: the gate proves an adult is present, and the dialog that follows is the last step before an irreversible send. Kept separate from `AiImageResult` so the result modal remains the reveal/download/refusal shell.                                                                                                                                                                                                                                     |
| `components/nav/`                    | `SidebarToc` — the guide-rail table of contents over one continuously scrolling document, shared by the wide Settings sidebar, `/design` and `/changelog`; the three surfaces differ only in their data (an item's `icon`, `meta`, `group`), and it deliberately has no `variant` prop. `TocDisclosure` is its narrow-screen counterpart: one sticky row whose collapsed state doubles as the scrollspy readout — the section count until the reader is inside a section, that section's name after — opening onto the same rail.                                                                                                                                                                   |
| `components/beta/`                   | Everything `/beta` is made of: `AndroidBetaPanel` and `IosBetaPanel` (each platform's four steps and its troubleshooting rows), the `BetaStepLedger`/`BetaStep`/`BetaTroubleshooting` geometry both wear, the `androidBeta.ts`/`iosBeta.ts` store facts the drift guards read, and `betaPlatform.ts` — the tab vocabulary (query key, labels, default) plus the pre-hydration `<head>` stamp that decides which panel paints. It imports nothing, so the E2E specs and the deprecated redirect routes can read it too.                                                                                                                                                                              |
| `components/page/`                   | `PageShell` + `RuleLabel` — shared chrome of the standalone pages (`/beta`, `/changelog`, `/feedback`, `/privacy`, and the admin console): ground, sheet, masthead (back link + `CrayonStrip` + wordmark), hero, and the `--page-*` palette everything nested inside reads. `ReleaseHistory.svelte` is the generated full release-note body used by `/changelog`. The palette resolves from the themed app tokens, so every page wearing the shell follows night mode — no route pins it to one theme (ADR-0071's 2026-08-10 amendment). `/design` wears its own shell (sticky header + scrollspy TOC in its route file) but signs itself with the same `BrandMark`.                                |
| `components/report/`                 | `ReportFields` — the feedback form's field set (kind picker, message, private-support note, device-info opt-in + preview, honeypot), shared by Settings' `ReportForm` (posts JSON to `/api/report`) and the `/feedback` page (posts to a form action). Every control carries the `name` the form action reads.                                                                                                                                                                                                                                                                                                                                                                                      |
| `components/styleguide/`             | The `/design` styleguide's section partials, composed by the route into three parts ordered most-reusable-first: Foundations (`ColorSections`, `TypeSections`, `ScaleSections`, `AssetSections`, `RecipeSections`), Components & chrome (`PrimitiveSections`, `ChromeSections`), Brand & voice (`VoiceSections`) — every token group, paper/palette/icons, primitives, live settings furniture, `app.css` chrome-class specimens, and a named index of the bespoke chrome with `ChromeMiniMap` location diagrams, all rendered from the real sources (`tokens.ts`, `palette.ts`, the `Icon` glob, the shipped components). Split so each file stays under the size ratchet; see the `design` skill. |
| `adminFormat.ts`                     | Admin-console presentation helpers for relative usage times and token-usage details.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `actionButtonLayout.ts`              | Actions Panel button-row geometry shared by two consumers that must agree: `ActionsPanel.svelte` caps the rendered button size and Settings' Button Size slider caps its range (`maxActionButtonScale`). Plus `publishActionPanelState`, the DOM writer that stamps hydrated UI state (`--action-btn-scale`, `data-drawer-open`, `data-off-*`, `data-brush`, `data-single-brush`, `data-no-actions`) onto the panel subtree; `app.html` owns the immutable pre-paint `<html>` seed (ADR-0040). Owns no `$state` — reads the state modules, driven by a reactive caller.                                                                                                                             |
| `aiCredential.ts`                    | Classifies parent-entered AI credentials and calls the matching access-code or BYO-key verification endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ai/credentials.ts`                  | Single source for which credential an AI request carries (BYO key, managed token, or the free installation id), shared by generation and reporting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ai/loadingCopy.ts`                  | Side-effect-free loading copy shared by the AI result UI and its production-flow tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ai/styles.ts`                       | AI style presets (names, prompts, icons) for the image-generation picker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `apiHeaders.ts`                      | Canonical header names for managed access tokens, BYO API keys, installation pseudonyms, the free-remaining response, and signed AI-report context.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `freeGenerations.ts`                 | Shared free-grant limit and wire-contract types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `appVersion.ts`                      | Guarded client-facing accessor for the build-injected application version.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `devHarness.ts`                      | Runtime gate that makes server-rendered `/dev/*` harness routes return 404 unless development or the public harness flag enables them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `errorLog.ts`                        | Shared uncaught-error log prefixes and generic fallback message for client, server, and render-boundary sinks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `fonts.ts`                           | Canonical Quicksand font-family name shared by font-loading consumers and drift checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `idb.ts`                             | Lazy, rejection-resetting IndexedDB database and typed single-store key/value helpers, plus best-effort persistent-storage requests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `idle.ts`                            | Cancellable idle-work scheduler with a timeout fallback for browsers without `requestIdleCallback`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `imagePrefetch.ts`                   | Deduplicated responsive-image cache warming and cancellation used by the Coloring Book Picker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `inviteLink.ts`                      | Canonical query-parameter name for AI access-token invite links.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `latestRequest.ts`                   | Abortable latest-request coordinator and shared submit-state/network-error vocabulary for supersedable form requests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `palette.ts`                         | Canonical ordered drawing palette, closed label vocabulary, and label-to-color lookup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `storageKeys.ts`                     | Single source of truth and closed type for every persisted storage key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `supportEmail.ts`                    | The one human-reachable address, split so it never appears literally in a prerendered document. Offered by each `/beta` panel's step 4 (composed after hydration) and by `/feedback`'s error state (rendered server-side, but only into an action's POST response — a GET never carries it).                                                                                                                                                                                                                                                                                                                                                                                                        |
| `siteUrl.ts`                         | `SITE_ORIGIN` — the canonical public origin, for the rare place the site's address must be shown rather than followed (`/beta` prints the `/feedback` address in full, since it may be read on one device and acted on from another). Links that only get clicked stay relative so previews reach themselves. `siteUrl.test.ts` guards it against the two literals it can't import: `app.html`'s `og:url` and `vite.config.ts`'s native API base.                                                                                                                                                                                                                                                   |
| `report.ts`                          | The feedback report's shared vocabulary — the `ReportKind` union, the two picker labels, and the message cap the textarea's `maxlength` and the server's truncation both read.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `api.ts`                             | Single helper (`apiUrl`) that prefixes paths with `__NATIVE_API_BASE__` on native, or leaves them relative on web.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `audio/drawingSound.ts`              | Plays pencil-scratch audio while drawing via Web Audio: MP3s are decoded once into `AudioBuffer`s, each stroke plays a looping `AudioBufferSourceNode` through a `GainNode`, and pointer speed modulates the gain (ramped to avoid clicks).                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `colorRing.ts`                       | Computes the selection-ring color for palette swatches (slightly darker than the swatch, or lighter for very dark colors).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `hexPickerLayout.ts`                 | The color picker's 9×9 palette (9 hue families × 9 shades) and its two static honeycomb arrangements — portrait (families as rows) and the landscape transpose (families as columns). `ColorPicker.svelte` renders both grids; CSS media queries pick one per orientation and trim positionally so the constrained axis drops shades, never hues (ADR-0048).                                                                                                                                                                                                                                                                                                                                        |
| `nativePlugin.ts`                    | Rejection-resetting lazy loader for Capacitor module namespaces; callers destructure plugin proxies only after awaiting to avoid promise assimilation of the proxies' synthetic `then` method.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `platform/index.ts`                  | `isNative()` and `getPlatform()` — reads the Capacitor global without importing `@capacitor/core`, so the module is safe to evaluate during SSR.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `platform/deviceInfo.ts`             | Opt-in, non-identifying browser/native device snapshot collector used by feedback reports.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `platform/deviceReport.ts`           | Shared device-report shape, ordered display labels, and server-side sanitization used by the feedback UI and endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `platform/haptics.ts`                | Threshold-confirmation feedback: lazy Capacitor haptics on native, with the Web Vibration API fallback on supported browsers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `platform/notchBand.ts`              | Pure safe-area, color, cutout-edge, and native status-bar decisions for the Notch Band across web and native targets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `platform/orientation.ts`            | Device orientation detection utilities.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `platform/safeArea.ts`               | DOM probe that resolves CSS safe-area environment insets to pixel measurements.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `plugins/deviceLock.ts`              | Typed web/native facade for the local `DeviceLock` plugin that reads iOS Guided Access or Android App Pinning state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `plugins/pencilEraser.ts`            | Typed facade and subscription lifecycle for the iOS-only local `PencilEraser` double-tap event, with an inert web fallback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `theme.ts`                           | Light/dark/system appearance plumbing: stamps `data-theme` on `<html>` (absent = system, so the `prefers-color-scheme` CSS in `app.css` drives it), keeps the `theme-color` meta on the resolved theme, and watches OS switches in system mode. The setting itself lives in `state/settings.svelte.ts`; the pre-paint stamp in `app.html` mirrors this convention.                                                                                                                                                                                                                                                                                                                                  |
| `storage.ts`                         | Dual-layer storage: synchronous reads from `localStorage` (fast, no async flash); on native, every write is also mirrored to Capacitor Preferences for durability. `hydrateDurableStorage()` restores settings on app launch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `secureStorage.ts`                   | Named client-held secrets (BYO provider key, managed access code, admin session token): Keychain/Keystore via `@aparajita/capacitor-secure-storage` on native, AES-GCM-encrypted IndexedDB on web.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pwa/updates.ts`                     | Registers the PWA service worker (deferred behind the stroke-count + idle gate on a first visit; immediately at idle on repeat visits) and checks for updates: a waiting worker that matches the deployed version activates silently (no reload); a stale page applies the reload only while the app is hidden with a blank canvas, so a visible session is never interrupted.                                                                                                                                                                                                                                                                                                                      |
| `server/config.ts`                   | Server-only accessors for private runtime configuration, including OpenAI and GitHub issue credentials.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `server/ai/provider.ts`              | Server-only: the provider-agnostic AI seam (ADR-0047) — the `AiImageProvider` interface (prompt + drawing in; image \| refusal \| error out) and the active-provider export. The only AI module routes may import.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `server/ai/openai.ts`                | Server-only: the OpenAI `AiImageProvider` adapter — the sole runtime importer of `openai`. Owns the orchestrator and image model ids, the effort tier, the toddler-safety system instruction, and the call itself. Goes through the Responses API image-generation tool so the model can decline in prose rather than draw (ADR-0113).                                                                                                                                                                                                                                                                                                                                                              |
| `server/ai/openaiSafety.ts`          | Server-only: pure classifier splitting an OpenAI response/error into image vs safety refusal vs empty (ADR-0023), plus the unverified-organization case a BYOK parent can actually act on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `server/ai/imageSize.ts`             | Server-only: reads a drawing's pixel dimensions out of its PNG/WebP header and maps the canvas shape onto the image size to render at, so a tall drawing is not composed onto a square canvas. Derived from the bytes rather than the request contract, so shipped native clients need no change.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `server/generationAuthorization.ts`  | Server-only authorization and rate-limit flow for free-grant, managed-token, and BYO-key image generation requests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `server/freeGenerationGrants.ts`     | Server-only Netlify Blobs grant ledger: conditional reservations/finalization, failure accounting, local fallback, and admin aggregates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `server/github.ts`                   | Server-only private GitHub issue-creation seam (ADR-0060), including untrusted Markdown escaping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `imageReport.ts`                     | Shared client/server report-kind vocabulary and retention duration for confirmed AI reports.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `server/imageReportAuthorization.ts` | Server-only validation and report-specific rate limits for AI reports on all three credentials, plus verification of credential-bound refusal context: managed token, BYO key, and the free tier's signed report token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `server/imageReport.ts`              | Server-only confirmed-AI-report core: validates the closed report kind, style, and required evidence; rebuilds the resolved prompt; stores the bundle; and sends its categorized private notification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `server/reportToken.ts`              | Server-only issue/verify for short-lived AI-report tokens: free-picture authorization and credential-bound, HMAC-authenticated refusal context carrying the provider reason (ADR-0104).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `server/imageReportStore.ts`         | Server-only Netlify Blobs evidence storage and paginated 30-day purge for confirmed picture and false-positive-refusal reports (ADR-0104).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `server/imageReportStoreName.ts`     | Side-effect-free name of the AI-report Blobs store, shared by production storage and the local `fetch:image-reports` tool.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `server/http.ts`                     | Shared server-request parsing, JSON error responses, and canonical throttling response/message helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `server/tokens.ts`                   | Server-only: validates and manages AI access tokens (stored in Netlify Blobs).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `server/admin.ts`                    | Server-only: admin auth core (secret check, derived session token, invite building) shared by the `/admin` page actions and the `/api/admin/*` endpoints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `server/report.ts`                   | Server-only: the feedback core — validation, honeypot, issue title/body Markdown, and the friendly error wording — shared by the `/api/report` endpoint and the `/feedback` form action. Rate limiting stays with each caller; both key the same `reportBucket`.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `server/rateLimit.ts`                | Server-only generic per-key in-memory sliding-window limiter, including read-only peeks for endpoints that charge only failed attempts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `server/rateLimitKeys.ts`            | Namespaced key builders that define which endpoint callers share or isolate rate-limit buckets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `server/rateLimitPolicy.ts`          | Central per-endpoint rate-limit thresholds and windows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `server/securityHeaders.ts`          | Canonical security-response headers for SSR, plus the exact-route referrer-policy override for `/feedback`; tests guard the duplicate Netlify configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `server/usage.ts`                    | Best-effort AI usage logging and Netlify Blobs tallies for managed tokens, including compare-and-set updates and revocation cleanup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `icons/`                             | SVG icon assets. `npm run gen:icon-names` generates `components/icon-names.d.ts` from them — the typed `IconName` union used by `<Icon>`. `<Icon>` inlines the raw SVG via `{@html}` (so the icon name lives only in a `data-icon` attribute, not the SVG), which means dynamic icons carry a hydration caveat — see `.claude/rules/svelte.md`.                                                                                                                                                                                                                                                                                                                                                     |
| `releases.json`                      | Auto-generated from `releases/*.md` by `npm run gen:releases`; consumed by the What's New section in Settings (drill-in header "Updates"; current notes, no version number) and the `/changelog` table of contents. The same generator compiles every release body into `components/page/ReleaseHistory.svelte` so the full history is static markup.                                                                                                                                                                                                                                                                                                                                               |

### `web/src/routes/`

Rendering mode is set per route via `prerender`/`ssr` page options; the site-wide default is
`prerender = true` in `+layout.ts`, and specific routes opt out. **Render** below: **SSG** =
prerendered to static HTML at build time (served from the CDN / baked into the native bundle; the
SSR function never runs for it); **SSR** = rendered per request by the `sveltekit-render` Netlify
function. See **ADR-0040** for the split and why `/` deliberately stays SSG (it has no per-request
personalization — user preferences hydrate client-side from `localStorage`, orientation from CSS
media queries + the head-script stamp in `app.html`).

| Route                        | Render | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/`                          | SSG    | Main app (drawing canvas, palette, controls). Prerendered shell, SPA client — all user state hydrates client-side.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/api/generate-image`        | SSR    | Serverless function (Netlify). Raw drawing bytes in, stylized image out — see the `api` skill for the full contract. Credential- or free-grant-gated and rate-limited. Not bundled for native.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `/api/free-generation-grant` | SSR    | Reads the server-authoritative remaining free generations for a privacy-preserving installation pseudonym; reports unavailable when the project key or durable daily free-provider budget cannot serve it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `/api/report-image`          | SSR    | Receives a gated, confirmed AI-picture or false-positive-refusal report, retains only its actionable evidence for 30 days, and notifies the private support channel. Generation-credential-gated and separately rate-limited.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/api/report`                | SSR    | Accepts an in-app feedback report, validates and sanitizes it, and opens a labelled private support issue. Unauthenticated and rate-limited per IP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/api/csp-report`            | SSR    | Receives size-capped CSP `report-uri` and Reporting API payloads, logs normalized violations to the Netlify function log, and rate-limits submissions per IP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/api/verify-access-code`    | SSR    | Validates an invite token string.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `/api/verify-key`            | SSR    | Validates a user-supplied OpenAI API key (BYO Key flow).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `/api/admin/*`               | SSR    | JSON twin of the `/admin` console (bearer-session auth), driven by `tools/api-smoke/lib/admin-client.mjs` in the smoke tests. See the `api` skill.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/admin`                     | SSR    | Token management console (`prerender = false` — cookie-authenticated form actions). Web-only and unlinked: reached by typing the URL. Nothing in the app points at it, so a store reviewer finds no hidden door to a privileged surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `/privacy`                   | SSG    | Static privacy policy page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `/changelog`                 | SSG    | Complete release history with a table of contents, generated from `releases/*.md`. Ships in both the web and native static exports so What's New links to it directly without an external-link parental gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/beta`                      | SSG    | Beta sign-up instructions for both stores behind Android and iPhone / iPad tabs (`SegmentedPicker`'s `underline` variant; the Android tab carries the robot Google permits in one color, the other a generic phone-and-tablet mark, since Apple's marketing guidance rules out its logo) — Google Play closed testing and TestFlight. `noindex`; built on `PageShell`. Both panels are in the document and CSS filters them off the platform an inline `<head>` script stamps on `<html>` (from `?os=`, else the device), so the right one paints before hydration and both stay readable with JavaScript off. Web-only in two layers: `prerender = !__IS_CAPACITOR__` drops its HTML from the native export, and `web/nativeExcludedRoutes.ts` blanks the module source so the store URLs never reach the bundle (`tools/mobile/check-static-bundle.mjs` gates it). |
| `/android-beta`, `/ios-beta` | SSR    | Deprecated solo pages, kept alive because both were handed out on their own: each 308s to its `/beta` tab (ADR-0112). `prerender = false` — prerendering a redirect writes a meta-refresh document and makes the prerenderer emit a second copy of `/beta` under a `?`-named file. The deployed site answers them at the edge from `netlify.toml` instead, so they cost no function invocation; `betaPlatform.test.ts` fails if the two redirect targets disagree.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/feedback`                  | SSR    | Standalone bug/idea form — the link-shareable twin of Settings' Send Feedback section, built on `PageShell` with the same `ReportFields`. `prerender = false` (it has a form action), so it is web-only. Every control is a real named form control and failures echo the submitted values back, so it works with JavaScript unavailable — which is also why `/feedback` is the one route `securityHeadersFor` relaxes to `Referrer-Policy: same-origin` (`no-referrer` opaques the Origin that SvelteKit's CSRF guard checks).                                                                                                                                                                                                                                                                                                                                      |
| `/design`                    | SSR    | Public living styleguide — voice, brand, every design token, and every primitive, in both themes (ADR-0071). `prerender = false` keeps it web-only: the native static export (`strict: false`) drops routes that opt out of prerendering. See the `design` skill.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `/dev`                       | SSR    | Index of the dev harnesses — links to each one. `prerender = false`; unlocked by `PUBLIC_ENABLE_DEV_HARNESS=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/dev/engine`                | SSR    | Drawing engine white-box harness — the bare shared tiled live surface plus debug controls and tile-compositing pixel helpers. `prerender = false`; unlocked by `PUBLIC_ENABLE_DEV_HARNESS=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `/dev/store-frames`          | SSR    | Live store-screenshot design surface: the marketing-frame compositions `gen:store-assets` renders, hot-reloading per store slot for design iteration. `./render` is the bare per-page screenshot surface the generator captures; `./assets/*` serves the committed app captures (`store-assets/captures/`) and showcase art from a closed file map. Same gate as the other dev harnesses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

---

## UI Elements

> **Layout notes (read before positioning a new control):**
>
> * The **Color Palette** is orientation-asymmetric (`ColorPalette.svelte`): a full-width **row
>   along the top edge** in portrait, a **column down the left edge** in landscape (single- or
>   two-column depending on height). So a "top-left corner" is *not* free space — it's under the
>   first swatch in one orientation.
> * **Float canvas-overlay controls inside `.canvas-container`** (`DrawingCanvas.svelte`,
>   `position: relative`), using `position: absolute`, rather than a fixed viewport corner — that
>   container already tracks the drawing area across orientations. The **Fullscreen Toggle**
>   (`top: 8px; left: 8px; z-index: var(--z-canvas-chrome)`) is the reference example. Note
>   `.canvas-container` sets up no stacking context, so such a control still competes with the
>   viewport chrome below — it just sits under all of it.
> * **Bottom edge is contested**: the **Settings Button** (`#settingsButton`,
>   `var(--z-corner-button)`, bottom-right, `SettingsButton.svelte`) and the **Actions Panel**
>   flyouts (`var(--z-panel)`/`var(--z-flyout)`, bottom-left, `ActionsPanel.svelte`) share it and
>   collide on small viewports — a bug that's invisible from either file alone. Check both when
>   changing sizes or breakpoints down there. Every `--z-*` value lives in `lib/design/tokens.ts`.

* **Color Palette** - Container bar holding all color swatches. **Top-edge row in portrait,
  left-edge column in landscape** (`ColorPalette.svelte`) — see Layout notes above.
  * **Color Swatch** - Individual circular color selection button
    * **Selection Ring** - Colored ring indicator around the active color swatch
* **Gradient Swatch** - Last color button, a honeycomb of palette-color hexagons (the `more-colors`
  icon) on the bar surface; opens custom color picker
  * **Color Picker Overlay** - Full-screen modal with blurred backdrop for selecting custom colors
  * **Hexagon Grid** - Honeycomb pattern of color tiles in the color picker
  * **Color Hexagon** - Individual hexagon-shaped color tile; drag across to explore, lift to select
* **Drawing Canvas** - Main touch-responsive drawing surface
* **Fullscreen Toggle** - Subtle top-left button that enters/exits immersive fullscreen to dismiss
  the mobile URL bar a non-scrolling canvas can never scroll away; the icon flips between expand and
  minimize. Opt-in only (never auto-triggered, so the chrome flicker is user-initiated) and shown on
  Android web browsers only — iOS Safari has no element fullscreen and the native shell is already
  fullscreen. See `docs/COMPATIBILITY.md`.
* **Waiting Polaroid** - The print pinned to the **top-left of the canvas** while an AI picture is
  being made and until it is looked at (`AiWaitingPolaroid.svelte`, ADR-0116/0117): the child's own
  drawing under a spinner with the run's progress as its caption, then the finished picture with a
  wand-stars badge and "Ready!". Tapping it is the way back into a minimized generation, so it
  outranks the Color Palette it is pinned beside (`--z-waiting-polaroid`) and is offset past it in
  both orientations — see the Layout notes above about that corner never being free space.
* **Notch Band** - Thin strip filling the device's top safe-area inset (the notch / hole-punch area
  behind the system clock), painted with the active drawing color and cleared to paper-white on the
  eraser. Hidden on devices without a real cutout (bezel iPads). See ADR-0026.
* **Clear Button** - Floating trash button for clearing the canvas
  * **Clear Preview Line** - Torn paper edge visual indicator showing where canvas will be cleared
    during drag
  * **Clear Accept Zone** - Bottom 15% of screen that turns red; drop Clear Button here to confirm
  * **Page Turn Overlay** - White overlay animation that sweeps across when clearing
* **Actions Panel** - Bottom-corner panel hosting auxiliary controls
  * **Brush Button** (`#brushButton`) - Adapts to the enabled optional brushes (ADR-0067): absent
    for none, a direct toggle between Pen and the lone tool for one, or a trigger opening the
    **Brush Menu** for two or more. The menu lists Pen followed by only the enabled optional tools:
    **Pen** (`#penBrushButton`) — solid ink, the default; **Crayon** (`#crayonBrushButton`) —
    textured wax (ADR-0065); **Magic Brush** (`#magicBrushButton`); **Eraser** (`#eraserButton`,
    hidden by the independent Settings toggles). The choice persists (`splotch-brush-type`; the
    eraser never does), and pen/crayon share the active palette color. The magic brush paints by
    revealing the applied coloring page's colored fill (`.light.webp`) where the child strokes — a
    `magic`-flagged op whose paint is a `CanvasPattern` of the fill (ADR-0043), or a random rainbow
    on a blank canvas. Magic ops are otherwise ordinary ops in the active/pending
    `StrokeGroupCommand`s. Ready operations render into live tiles; older commands compact into the
    tiled history base, and tile-local patches preserve undo (ADR-0085/0086).
  * **Undo Button** - Reverts the last drawing stroke
  * **Screenshot Button** - Saves the current drawing as a PNG (toggle in Settings)
  * **Stroke Width Button** - Opens a flyout for selecting line thickness (toggle in Settings)
  * **Coloring Book Button** - Opens the Coloring Book Picker (toggle in Settings)
* **Coloring Book Picker** - Modal dialog for choosing a coloring page to use as a canvas overlay
  * **Coloring Book Grid** - First menu showing each coloring book by its cover image
  * **Coloring Book Tile** - Individual book cover button; tap to open that book's pages
  * **Coloring Page Grid** - Second menu showing the 6 selectable coloring pages in a book
  * **Coloring Page Tile** - Individual coloring page; tap to apply it as the canvas overlay
  * **Coloring Page Overlay** - Selected page rendered behind the drawing canvas with multiply
    blend, so white areas blend into the paper background and the line art stays visible. Lives
    inside the **Paper View** wrapper (`.paper-view`, `DrawingCanvas.svelte`): full-container
    normally, but after a device rotation with ink on the canvas it contain-fits the locked paper
    upright and centered (ADR-0050) so the page and strokes move as one sheet — the tall/wide art
    variant is keyed off `canvasState.paperOrientation`, not the live viewport. The **Paper Sheet**
    (`.paper-sheet`, same transform) carries the off-white paper texture *beneath* the
    always-transparent canvas, so while rotated the original page reads as a distinct sheet (soft
    shadow, flat greyer container margins around it — no border). Those letterbox margins are
    outside the paper and reject pointer starts (ADR-0089).
* **Install Banner** - Friendly bottom-center pill prompting "Add Splotch to your home screen",
  shown on web after the child has drawn a few strokes. One-tap native install on Chromium/Android;
  guided Share-sheet hint on iOS. Dismissible and remembered. See ADR-0039.
* **Parental Gate** - "Grown-Ups Only" modal (`ParentalGate.svelte`) guarding individual sensitive
  operations at their boundary — never Settings entry itself (ADR-0094). An adult solves a random
  single-digit multiplication problem on its keypad (wrong answers regenerate the problem). The
  protected operations are AI image launch, AI result reporting, external-link activation, feedback
  submission, and Parent Center entry; Parent Center gives each an independent Every time / Per
  session / Never policy. The gate is an app-store requirement, so the default follows the build:
  the native build arms all five at Every time, the web build ships them all at Never and treats
  each as a parent's opt-in (`DEFAULT_PARENTAL_GATE_MODE`, off `__IS_CAPACITOR__`). Native iOS keeps
  external links' Never choice visible but unavailable and explains the Kids Category constraint
  inline; web and Android allow it. Policy selections persist, while per-session solves are
  in-memory. External-link handoff runs immediately inside the solving tap so browser user
  activation survives. Every challenge but Parent Center's own carries a footer naming where these
  checks are configured; activating it retargets the open card at Parent Center — same problem, new
  destination, and the copy says so — rather than stacking a second challenge on the first. "Parent
  Center entry" is a drill-in tap on the phone shell; in the wide shell every section is one scroll
  away, so `ParentCenterLock.svelte` stands in for that section's controls until the gate is solved.
* **Settings Button** - Floating button that opens Settings
  * **Settings** - Modal for app settings, install guides, and about info. Its body is one flat list
    of **Sections** (ADR-0061), not tabs: Appearance, Sound, Tool Drawer, Coloring, AI Art, Saving,
    Parent Center, Install, Feedback, What's New (drilled-in header: "Updates"), About. Both shells
    render from the same `SECTIONS` list in `settings/sections.ts`, chosen by viewport width
    (`SettingsModal.svelte`): below ~700px a **Hub** list drills into a full-page section with a
    back arrow; at/above ~700px (`settings/WideShell.svelte`) a persistent **Sidebar** (its own
    scroller whenever the section list outgrows the column; scroll-position edge shades mark it, and
    a reopen resets its scroll) sits beside a scrolling content **Pane** that holds *every* section
    at once in nav order — the Sidebar is a scrollspy-driven table of contents that moves the Pane's
    scroll position rather than choosing what renders, so its highlight is an indicator
    (`aria-current="location"`, a brand wash with a left rail), not a page state, and the Sidebar
    scrolls the spied row back into its own column when the Pane elects one that is off screen
    (ADR-0061). Each section component lives in `settings/` (`AppearanceSection`, `SoundSection`,
    `SavingSection`, `ColoringSection`, `ControlsSection`, `AiKeyManager`, `ParentCenterSection`,
    `SetupInstructions`, `WhatsNewSection`, `ReportForm`, `AboutSection`).
    * **Hub Row** - one section per row: icon, title, and a live status subtitle
      (`sectionSubtitle`). No chevrons. The two rows a parent flips mid-session — Appearance's
      **Night Mode** and Sound — are split rows instead: the body still drills in, and a trailing
      switch (`settings/ToggleSwitch.svelte`, shared with `ToggleRow`) acts on the spot, separated
      by a hairline. Those two lead the list, and an extra gap sets them off from the drill-ins
      below (ADR-0061 amendment).
    * **Section Activity Dot** - tiny static brand dot on a Hub Row or Sidebar icon when that
      section has never been read at its current content stamp. Seen stamps are recorded from the
      first app session, but dots stay hidden until session six; opening the phone drill-in,
      selecting the Sidebar row, or scrolling its section into the wide Pane clears and persists the
      dot. What's New uses the app version as its stamp so each release marks it new again.
    * **Install Guide** - step-by-step PWA setup inside the **Install** section, for the one device
      family `installDeviceOs()` detects (iOS, Android, or desktop — the last with no app-lock
      steps, since neither Guided Access nor App Pinning has a desktop analogue), plus the one-tap
      install button when the browser supports it
    * **Tool Drawer Section** - Enable Advanced Controls toggle, a **Button Size** slider that
      rescales the Actions Panel buttons (dragging it melts the rest of Settings away so the buttons
      resize in full view), and a **Drawing Tools** list that toggles each brush and Actions Panel
      button on/off — brushes first, then the controls beside them. The list wears one of two skins,
      chosen by the block's own measured width: a 2-column **chip grid** where two chips fit, and
      the same plain **toggle rows** the rest of Settings uses wherever the block is down to one
      column. Either way it is one exported list (`settings/drawingTools.ts`), which the hub row's
      "n tools hidden" subtitle counts. The camera button's own visibility lives in **Saving**
      instead, leading that section above Auto-Save on Delete.
      * **Appearance Control** - Light / Dark / System segmented control at the top of the
        **Appearance** section. Dark mode themes the chrome (app background, palette bar, modals,
        Install Banner), the paper (a near-black warm tone under the same low-alpha texture), and
        the paper-floating controls (Actions Panel/flyout on `--float-surface` with a
        `--float-border` edge; near-black ink gets a `--dark-ink-keyline` ring) via the tokens in
        `app.css`. **Coloring pages stay on the dark paper** — the line art inverts to white "chalk"
        lines via `--lineart-filter`/`--lineart-blend`, and the magic brush reveals a parallel set
        of pre-colored **night fills** (`{page}-{orient}.night.webp`; light fill in light mode,
        night fill in dark, picked by `resolvedTheme()`, falling back to the light fill where a
        night asset isn't generated yet). Exports follow the resolved theme (a dark save is the
        night version). Only the Clear Button keeps its literal red chrome. System (the default)
        follows the OS via `prefers-color-scheme` with no `data-theme` attribute stamped. See
        ADR-0052.

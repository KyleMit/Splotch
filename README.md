# pr-assets

Image host branch for pull-request screenshots. **Never merged into `main`.**

Files here are referenced from PR bodies via `raw.githubusercontent.com` URLs.
Each PR gets its own folder. See `.claude/skills/pr-screenshots/SKILL.md`.

{
  "name": "splotch",
  "version": "1.4.0",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "info": "scripts-info",
    "dev": "node scripts/web.mjs vite dev",
    "predev:netlify": "node scripts/check-netlify-cli.mjs",
    "dev:netlify": "netlify dev --cwd web",
    "dev:host": "node scripts/web.mjs vite dev --host",
    "dev:cap": "CAPACITOR=true node scripts/web.mjs vite dev --host",
    "dev:tunnel": "node scripts/cloud-tunnel.mjs",
    "dev:kill": "npx kill-port 5173 8888",
    "prebuild": "node scripts/web.mjs svelte-kit sync && npm run gen:icons && npm run gen:releases",
    "build": "node scripts/web.mjs vite build",
    "prebuild:cap": "node scripts/web.mjs svelte-kit sync && npm run gen:icons && npm run gen:releases",
    "build:cap": "CAPACITOR=true node scripts/web.mjs vite build && node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/strip-native-assets.mjs",
    "preview": "node scripts/web.mjs vite preview",
    "precheck": "node scripts/check-native-app-id.mjs && node scripts/web.mjs svelte-kit sync",
    "check": "node scripts/web.mjs svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "check:watch": "node scripts/web.mjs svelte-check --tsconfig ./tsconfig.json --watch",
    "check:assets": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/check-assets.mjs",
    "check:assets:manifest": "node tools/asset-gen/bin/gen-asset-manifest.mjs --check",
    "img:audit": "node scripts/image-audit.mjs",
    "img:audit:check": "node scripts/image-audit.mjs --check",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:tokens": "node scripts/lint-token-styles.mjs",
    "lint:dead": "knip --no-progress --include files,exports,types,duplicates",
    "format": "prettier --write . && npm run format:md",
    "format:check": "prettier --check . && npm run format:md:check",
    "format:md": "dprint fmt",
    "format:md:check": "dprint check",
    "ruler:apply": "node scripts/ruler-apply.mjs",
    "ruler:check": "node scripts/ruler-check.mjs",
    "ruler:dry-run": "ruler apply --dry-run --verbose",
    "test": "npm run test:unit && npm run test:asset-gen && npm run test:scripts && npm run test:e2e",
    "perf:web": "PERF_MARKS=true node scripts/perf/scenario.mjs",
    "perf:web:raw": "PERF_MARKS=true node scripts/perf/scenario.mjs --no-throttle",
    "perf:mount": "PERF_MARKS=true node scripts/perf/mount.mjs",
    "perf:android": "PERF_MARKS=true node scripts/perf/android.mjs",
    "perf:ios": "PERF_MARKS=true node scripts/perf/ios.mjs",
    "preperf:ipad": "npm run perf:build",
    "perf:ipad": "PERF_MARKS=true PUBLIC_ENABLE_DEV_HARNESS=true node scripts/perf/ipad.mjs",
    "perf:undo": "PERF_MARKS=true node scripts/perf/undo-scenarios.mjs",
    "perf:undo:webkit": "PERF_MARKS=true node scripts/perf/undo-scenarios.mjs --engine=webkit --no-throttle",
    "perf:replay": "PERF_MARKS=true node scripts/perf/replay-scenario.mjs",
    "perf:build": "PERF_MARKS=true PUBLIC_ENABLE_DEV_HARNESS=true npm run build",
    "preperf:serve": "npm run perf:build",
    "perf:serve": "PUBLIC_ENABLE_DEV_HARNESS=true node scripts/perf/serve.mjs",
    "perf:analyze": "node scripts/perf/analyze.mjs",
    "perf:ios:analyze": "node scripts/perf/analyze-webinspector.mjs",
    "test:unit": "node scripts/web.mjs vitest run",
    "test:unit:watch": "node scripts/web.mjs vitest",
    "test:asset-gen": "vitest run --config tools/asset-gen/vitest.config.mjs",
    "test:scripts": "vitest run --config scripts/vitest.config.mjs",
    "test:e2e": "node scripts/web.mjs playwright test",
    "test:e2e:ui": "node scripts/web.mjs playwright test --ui",
    "test:e2e:headed": "SLOWMO=500 node scripts/web.mjs playwright test --headed",
    "test:e2e:debug": "node scripts/web.mjs playwright test --debug",
    "test:e2e:install": "node scripts/web.mjs playwright install",
    "test:android": "node scripts/android-emulator-smoke.mjs",
    "test:android:device": "maestro test .maestro/smoke.yaml",
    "test:ios": "node scripts/ios-simulator-smoke.mjs",
    "test:api:smoke": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/api-smoke.mjs",
    "test:blobs:smoke": "node scripts/blobs-smoke.mjs",
    "test:driver:smoke": "node scripts/driver-smoke.mjs",
    "redteam": "node scripts/redteam-run.mjs",
    "redteam:encrypt": "node scripts/redteam-fixtures.mjs encrypt",
    "redteam:decrypt": "node scripts/redteam-fixtures.mjs decrypt",
    "model-eval": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/model-eval-run.mjs",
    "model-eval:fixtures": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/model-eval-fixtures.mjs",
    "model-eval:gen-inputs": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/model-eval-gen-inputs.mjs",
    "gen:icons": "node scripts/generate-icon-names.mjs",
    "gen:releases": "node scripts/generate-releases.mjs",
    "gen:tokens": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/gen-tokens.mjs",
    "gen:tokens:check": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/gen-tokens.mjs --check",
    "gen:large-image": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/gen-large-image.mjs",
    "gen:shots": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/store-shots.mjs",
    "gen:style-covers": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/asset-gen/bin/gen-style-covers.mjs",
    "gen:coloring-chalk": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/asset-gen/bin/gen-coloring-chalk.mjs",
    "gen:coloring-fills": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/asset-gen/bin/gen-coloring-fills.mjs",
    "gen:coloring-fills:audit": "node tools/asset-gen/bin/check-coloring-drift.mjs",
    "gen:coloring-fills:audit:eyes": "node tools/asset-gen/bin/audit-fill-eyes.mjs",
    "gen:coloring-fills:audit:eyes:review": "node tools/asset-gen/bin/review-orb-eyes.mjs",
    "gen:coloring-fills:audit:shapes": "node tools/asset-gen/bin/audit-invented-shapes.mjs",
    "gen:coloring-fills:audit:halo": "node tools/asset-gen/bin/audit-night-halo.mjs",
    "gen:coloring-outlines:audit": "node tools/asset-gen/bin/audit-outline-solidity.mjs",
    "gen:coloring-outlines:fresh": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/asset-gen/bin/gen-coloring-outlines-fresh.mjs",
    "gen:coloring-outlines:normalize": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/asset-gen/bin/normalize-outline-strokes.mjs",
    "gen:coloring-golden:freeze": "node tools/asset-gen/bin/audit-golden.mjs --freeze",
    "gen:coloring-golden:diff": "node tools/asset-gen/bin/audit-golden.mjs --diff",
    "gen:coloring-punch": "node tools/asset-gen/bin/punch-fill-outlines.mjs",
    "gen:coloring-thumbs": "node tools/asset-gen/bin/gen-coloring-thumbs.mjs",
    "gen:coloring-book-proof-sheet": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/asset-gen/bin/gen-coloring-book-proof-sheet.mjs",
    "gen:icons-sheet": "node scripts/gen-icons-sheet.mjs",
    "gen:e2e-tuning-report": "node scripts/gen-e2e-tuning-report.mjs",
    "gen:assets:manifest": "node tools/asset-gen/bin/gen-asset-manifest.mjs",
    "scrapbook:publish": "node scripts/publish-scrapbook.mjs",
    "scrapbook:index": "node scripts/publish-scrapbook.mjs --index-only",
    "scrapbook:proof-sheet-hub": "node scripts/gen-coloring-book-proof-sheet-hub.mjs",
    "scrapbook:check": "node scripts/publish-scrapbook.mjs --check",
    "cap:sync": "npm run build:cap && cap sync",
    "cap:android": "npm run cap:sync && cap open android",
    "cap:ios": "npm run cap:sync && cap open ios",
    "android:setup": "node scripts/android-setup.mjs",
    "android:boot": "node scripts/android-emulator.mjs boot",
    "android:emulator": "node scripts/android-emulator.mjs emulator",
    "android:live": "node scripts/android-emulator.mjs live",
    "android:apk": "npm run cap:sync && node scripts/gradle.mjs :app:assembleDebug",
    "android:run": "npm run cap:sync && node scripts/gradle.mjs :app:installDebug",
    "android:run:device": "npm run cap:sync && ANDROID_SERIAL=R5CY128YMGF node scripts/gradle.mjs :app:installDebug",
    "android:bundle": "npm run cap:sync && node scripts/gradle.mjs :app:bundleRelease",
    "android:verify": "node scripts/android-verify.mjs",
    "android:open": "node scripts/android-open.mjs",
    "android:clean": "node scripts/gradle.mjs clean",
    "ios:build": "npm run cap:sync && cd ios/App && xcodebuild -scheme App -configuration Debug -destination \"generic/platform=iOS Simulator\" -derivedDataPath build build",
    "ios:run": "npm run cap:sync && cap run ios",
    "ios:run:emulator": "npm run cap:sync && cap run ios --target C6012C49-AA93-4869-B3A6-E47C9EAAC567",
    "ios:run:device": "npm run cap:sync && cap run ios --target 00008103-0006202E3CF1001E",
    "ios:live": "cap run ios --live-reload --port 5173",
    "ios:archive": "npm run cap:sync && cd ios/App && xcodebuild -scheme App -configuration Release -destination generic/platform=iOS -archivePath build/App.xcarchive -xcconfig ../local.xcconfig -allowProvisioningUpdates archive",
    "ios:ipa": "npm run ios:archive && cd ios/App && xcodebuild -exportArchive -archivePath build/App.xcarchive -exportOptionsPlist ExportOptions.plist -exportPath build/ipa -allowProvisioningUpdates",
    "ios:open": "node scripts/open-path.mjs ios/App/build/ipa",
    "ios:clean": "cd ios/App && xcodebuild -scheme App clean",
    "adb:devices": "adb devices",
    "adb:reverse": "adb reverse tcp:5173 tcp:5173",
    "deps:gha": "node scripts/gha-versions.mjs",
    "audit:preflight": "node scripts/audit-burndown/preflight.mjs",
    "audit:burndown": "node scripts/audit-burndown/burndown.mjs",
    "audit:burndown:overnight": "node scripts/audit-burndown/overnight.mjs",
    "audit:status": "node scripts/audit-burndown/status.mjs",
    "audit:cost": "node scripts/audit-burndown/cost.mjs",
    "audit:watch": "node scripts/audit-burndown/watch.mjs",
    "prerelease": "npm run check:assets && npm run check:assets:manifest",
    "release": "node scripts/release.mjs",
    "release:publish": "node scripts/publish-artifacts.mjs"
  },
  "scripts-info": {
    "info": "Lists every npm script with its description (this output)",
    "dev": "Dev server at localhost:5173 (no /api functions)",
    "predev:netlify": "Checks the Netlify CLI is on PATH, logged in, and linked before dev:netlify (Blobs needs auth), with a hint if not",
    "dev:netlify": "Dev server with the /api/* serverless functions (Netlify Dev)",
    "dev:host": "Dev server exposed on the local network (for phones and other devices)",
    "dev:cap": "Dev server in native mode (CAPACITOR=true) on the network, for android:live",
    "dev:tunnel": "Claude Code Cloud only: start dev + a chisel reverse tunnel and print the public phone-preview URL (needs TUNNEL_AUTH; see docs/CLOUD/Claude.md, ADR-0021)",
    "dev:kill": "Kill any stale dev servers holding the dev ports (vite 5173, netlify dev 8888)",
    "prebuild": "svelte-kit sync + gen:icons + gen:releases before build",
    "build": "Web build (adapter-netlify) for splotch.art",
    "prebuild:cap": "svelte-kit sync + gen:icons + gen:releases before build:cap",
    "build:cap": "Native static build (CAPACITOR=true, adapter-static); strips web-only coloring-book assets",
    "preview": "Serve the production web build locally",
    "precheck": "Verify every native/tooling app ID matches canonical capacitor.config.json.appId, then generate .svelte-kit types (svelte-kit sync) so type checking can run standalone",
    "check": "svelte-check / type checking (one-shot; warnings fail too, so compiler hints like non_reactive_update gate CI)",
    "check:watch": "svelte-check in watch mode",
    "check:assets": "Validate that every coloring-book asset exists on disk (auto-runs before release)",
    "check:assets:manifest": "CI guard for gen:assets:manifest: fail if any committed art asset's bytes drifted from tools/asset-gen/golden/asset-manifest.sha256 (e.g. a night pass touched a light file); run gen:assets:manifest and commit to fix",
    "img:audit": "Optimize every shipped/inlined SVG under web/ in place with SVGO (visually-lossless preset; keeps viewBox). Idempotent — re-running only rewrites new/edited SVGs. Skips generator-input SVGs (large-image.svg, styles/source.svg)",
    "img:audit:check": "CI guard for img:audit: fail if any SVG under web/ isn't already optimized (drift detector; run img:audit and commit to fix)",
    "lint": "ESLint over the whole repo minus the ignores in eslint.config.js (flat config; enforces runes-only, no legacy stores)",
    "lint:fix": "ESLint with autofix",
    "lint:tokens": "Token-style ratchet for component <style> blocks (ADR-0071): fails if any file's raw hex-color count drifts from the allowlisted baseline in scripts/lint-token-styles.mjs, or a raw 2+-digit z-index bypasses the --z-* tokens",
    "lint:dead": "Dead-code gate (knip): fails on unused files, exports, and exported types across web/src, scripts, and tools/asset-gen — remove the symbol or tag a deliberate seam with /** @public */",
    "format": "Format the whole repo: Prettier for source and config files, dprint for markdown",
    "format:check": "Check Prettier + dprint formatting without writing — what CI runs",
    "format:md": "Format all markdown docs with dprint (asterisk bullets/emphasis, wrap at 100, ADR-0057)",
    "format:md:check": "Check markdown formatting without writing — part of format:check",
    "ruler:apply": "Regenerate agent files from .ruler/ while preserving direct provider skill packages, apply managed skill forks, then dprint-format the committed output (ADR-0058)",
    "ruler:check": "CI drift guard for ruler:apply: re-apply and fail if generated agent output changed; excludes the direct provider-specific burn-down-audits packages",
    "ruler:dry-run": "Preview what ruler:apply would regenerate without writing any files (note: skips the dprint fmt pass, so blank-line noise it reports may format away)",
    "test": "App unit + asset-pipeline unit + repo-script unit (Vitest) + E2E (Playwright) — what CI runs on every push",
    "perf:web": "Autonomous performance profile of the web app: builds the production bundle with PERF_MARKS=true, serves it via vite preview, drives a scripted toddler session (multi-finger draw, color/size changes, erase, undo, clear) in headless Chromium under 4x CPU throttle while recording a Chrome trace + runtime metrics, then writes trace.json/metrics.json/summary.json/report.md + a screenshot to perf-profiles/. Flags: --device=phone|tablet|desktop, --throttle=N, --no-build (reuse last build)",
    "perf:web:raw": "perf:web without CPU throttling (raw desktop speed)",
    "perf:mount": "Autonomous page-load (mount) profile — the Lighthouse-TBT window scenario.mjs skips: builds the production bundle, then records a Chrome trace ACROSS the initial navigation in headless Chromium (phone viewport, 4x CPU throttle, Slow-4G network emulation), capturing load-phase long tasks via a buffered longtask observer plus paint timings. Writes trace.json (feed to perf:analyze) + mount-summary.json to perf-profiles/. Flags: --device=phone|tablet|desktop, --throttle=N, --no-throttle, --no-build",
    "perf:android": "Autonomous performance profile of the REAL Capacitor WebView on a connected Android emulator/device (local-only): rebuilds + installs the native app with PERF_MARKS=true, finds the WebView's DevTools socket, forwards it, connects Playwright over CDP, and runs the same toddler session + capture as perf:web (no CPU throttle — the device is the target). --no-build profiles the installed app as-is. Writes to perf-profiles/",
    "perf:ios": "Autonomous performance profile in Playwright's WebKit (the same WebKit+JavaScriptCore engine the iOS WKWebView runs), driving the production preview build. WebKit has no CDP trace, so this captures the engine.* user-timing marks + FPS (not the main-thread/CPU breakdown); it profiles the engine, not the Simulator app. For device-accurate numbers, record a Safari Web Inspector Timeline against the running Simulator and feed it to perf:analyze. Writes to perf-profiles/",
    "perf:undo": "Focused undo profile (ADR-0066 snapshot stack): builds the production bundle with PERF_MARKS=true, serves it via vite preview with the /dev/engine harness unlocked, and drives seven deliberately-shaped sessions (long squiggles; short dot/dash marks; a mix; five-finger drags; pen back-and-forth scribbles; crayon squiggles; crayon scribbles with mid-stroke pass splits) in headless Chromium under 4x CPU throttle. Records per scenario — via getUndoDebug() — snapshot depth, live rasters vs encoded blob bytes, plus engine.draw/commit/snapshot/fold/encode/undo timing and history memory while resident. Writes trace.json/summary.json/report.md + undo-scenarios.json/.md to perf-profiles/. Flags: --engine=chromium|webkit, --throttle=N, --no-throttle, --no-build, --scenarios=key1,key2 (subset)",
    "perf:undo:webkit": "The same undo scenarios as perf:undo, driven in Playwright's WebKit — the engine family the iOS app ships (WebKit/JavaScriptCore), where per-engine canvas API behaviour that Chromium cannot reproduce at any precision becomes visible (the synchronous toBlob encode behind #635). Enforces the commit gate: a scenario whose engine.commit max exceeds 25 ms exits non-zero, since a commit that hot is doing full-raster work on the pointerup path. No CDP on WebKit, so there is no Chrome trace, no CPU throttle, and no JS-heap table — the engine.* user-timing marks and the gates come through. Same flags as perf:undo",
    "perf:replay": "Replay a REAL finger-input recording (captured on-device with scripts/perf/ipad-recorder.js) through the engine under the profiler, so the harness measures actual human strokes instead of synthetic squiggles: opens /dev/engine, sizes the canvas to the recorded device, replays the pointer stream + UI actions at recorded timing (real frame pacing), captures a CDP trace + engine marks (draining the async undo queue so deep-undo measures land in the trace), and reports how your input landed on the snapshot stack (peak/end snapshot depth + blob KB via getUndoDebug, plus engine.snapshot/fold/undo hot-path rows). npm run perf:replay -- --recording=<file.json> [--turbo] [--throttle=N] [--no-build]. Writes trace/report + replay-summary to perf-profiles/",
    "perf:build": "Production web build instrumented for profiling: PERF_MARKS=true bakes in the engine.* performance.mark/measure calls and keeps function names through minification (web/vite.config.ts). Runs automatically as preperf:serve; run it directly to refresh the bundle a native or manual flow will serve",
    "preperf:ipad": "npm run perf:build — so perf:ipad always drives a freshly instrumented bundle rather than whatever build happens to be on disk. Skip the rebuild with: npm run perf:ipad --ignore-scripts",
    "perf:ipad": "Autonomous ADR-0066 gates run on a REAL USB-connected iPad (local-only): serves the instrumented build on the LAN, attaches to Safari over the WebKit Inspector Protocol via ios_webkit_debug_proxy (brew install ios-webkit-debug-proxy — Safari has no CDP on a device), navigates the tab to /dev/engine so the bundle is never stale, injects scripts/perf/ipad-console-driver.js, and reads back the table plus the driver's warnings. This is the automated form of Approach A in the profiling skill's ipad-device-profiling.md; the Timeline run there stays manual. Needs the iPad unlocked, trusting this Mac, on the same Wi-Fi, with Safari open on a tab and Web Inspector enabled. Flags: --scenarios=key1,key2, --strokes=N, --ops=N, --url=, --port=N, --device-id=, --no-serve. Writes ipad-gates.json to perf-profiles/",
    "preperf:serve": "npm run perf:build — so perf:serve always hands the iPad a freshly instrumented bundle rather than whatever build happens to be on disk. Skip the rebuild with: npm run perf:serve --ignore-scripts",
    "perf:serve": "Serve the current production build on the LAN (0.0.0.0:4173) with the /dev/* harness routes unlocked (PUBLIC_ENABLE_DEV_HARNESS=true — read at runtime, so it must be set on the server, not the build), so a real iPad/phone on the same Wi-Fi can reach it to record real input (scripts/perf/ipad-recorder.js) or drive /dev/engine. Rebuilds first via preperf:serve, then prints the reachable Network + Harness URLs — vite advertises one per bound interface including unreachable link-local (169.254.x.x) ones, so scripts/perf/serve.mjs filters them out. Stop it before perf:replay (same port). See the profiling skill's ipad-device-profiling.md",
    "perf:analyze": "Re-run the trace analyzer over a saved profile dir or trace.json (regenerates summary.json + report.md without re-capturing — the path a native-exported trace takes): npm run perf:analyze -- perf-profiles/<dir>",
    "perf:ios:analyze": "Analyze a Safari Web Inspector Timeline export from a REAL iOS device (a different format from perf:analyze — {recording:{records,markers}}, mark-only, ring-buffered, ~1ms clock). Recovers per-op main-thread cost (undo/snapshot/fold/commit/resize/draw) from the enclosing timeline record — engine.undo pairs an explicit end mark so async deep undos attribute fully — plus paint/composite and frame stats: npm run perf:ios:analyze -- <export>.json. See the profiling skill's ipad-device-profiling.md",
    "test:unit": "Unit tests, one-shot (Vitest, happy-dom)",
    "test:unit:watch": "Vitest watch mode",
    "test:asset-gen": "Asset-gen pipeline tests (Vitest, Node env): image-analysis helpers against committed fixtures plus mocked generator CLI workflows — no Gemini/network",
    "test:scripts": "Repo-automation script tests (Vitest, Node env, scripts/tests/): audit-burndown state/runner seams, complete Ruler skill-fork replacement, the Android emulator API-level drift invariant (every literal must match ANDROID_API_LEVEL in scripts/lib/android.mjs), and the throwaway-server env guard (both test servers must declare every private env var web/src reads instead of inheriting it from web/.env)",
    "test:e2e": "Playwright E2E tests, headless",
    "test:e2e:ui": "Playwright UI mode",
    "test:e2e:headed": "Playwright with browser visible, slowed down (SLOWMO=500)",
    "test:e2e:debug": "Playwright inspector",
    "test:e2e:install": "Install the Playwright browsers (one-time)",
    "test:android": "Android smoke test, self-contained: boots a headless emulator, builds + installs, runs Maestro, tears down",
    "test:android:device": "Maestro smoke test against an already-running emulator/device (what CI uses; same flow works on a booted iOS simulator)",
    "test:ios": "iOS smoke test, self-contained: boots a simulator, builds + installs, runs Maestro, tears down (macOS-only)",
    "test:api:smoke": "API contract smoke test, self-contained: boots a throwaway vite dev with test env, exercises the CORS/preflight contract + the admin auth flow + a public oracle against the documented /api/* shapes, tears down (no Gemini/Blobs needed)",
    "test:blobs:smoke": "Deploy smoke test (ADR-0025): against a real Netlify deploy (BLOBS_SMOKE_URL + ADMIN_ACCESS_TOKEN), asserts persistent:true and round-trips a token through Blobs to prove the deployed function has the Blobs context; cleans up after itself",
    "test:driver:smoke": "App-driver smoke test: boots the app and drives scripts/lib/app-driver.mjs (openAppPage + expandDrawer + pickColor + setStrokeSize + drawStroke) against current markup, so the uncovered gen:* drivers can't rot silently on selector/import changes",
    "redteam": "MANUAL AI-safety red-team (ADR-0023): boots a throwaway vite dev, decrypts the corpus, sends each safe-*/block-* drawing to a real Gemini call, and writes an HTML input->output report to tests/redteam/output/ for human review (opens in your browser). Pass ids to run a subset (npm run redteam -- block-gun) to iterate on one drawing. Real GEMINI_API_KEY + REDTEAM_FIXTURE_KEY required; excluded from npm test by design",
    "redteam:encrypt": "Encrypt tests/redteam/source/*.png -> tests/redteam/encrypted/*.enc (commit the .enc); needs REDTEAM_FIXTURE_KEY",
    "redteam:decrypt": "Decrypt tests/redteam/encrypted/*.enc -> tests/redteam/decrypted/ (gitignored); needs REDTEAM_FIXTURE_KEY",
    "model-eval": "MANUAL image-model bake-off: A/B-compares the two candidate production image models over web/tests/model-eval/inputs/ using the exact production request config, and writes a self-contained quality/cost/latency report to web/tests/model-eval/output/<runId>/report.html. Real GEMINI_API_KEY required; excluded from npm test. Env: SAMPLES, FILTER (id substring), CONCURRENCY, OUT_TAG, SKIP_REPORT; RESUME=<run dir> fills only the missing/failed cells of an existing run (never regenerates an image already on disk); REPORT_FROM=<run dir> (+ optional VERDICT_FILE) rebuilds report.html from an existing results.json with no API calls",
    "model-eval:fixtures": "Regenerate the ~45 local eval input drawings (coloring-book/night/magic-brush/scribble/freehand categories) from the real coloring assets + palette; deterministic. Env: FILTER, DEBUG_SAMPLE. Leaves gen__* (Gemini-authored) inputs intact",
    "model-eval:gen-inputs": "Author a few canvas-plausible input drawings with gemini-3.1-flash-image (gen__* prefix) and add them to the eval corpus; needs GEMINI_API_KEY",
    "gen:icons": "Generate src/lib/components/icon-names.d.ts (typed IconName union) from the SVGs in src/lib/icons/ (auto-runs before build)",
    "gen:releases": "Generate src/lib/releases.json + fastlane store changelogs from releases/*.md (auto-runs before build)",
    "gen:tokens": "Generate web/src/tokens.css (the design-token custom properties, light + dark) from the source of truth in web/src/lib/design/tokens.ts (ADR-0071)",
    "gen:tokens:check": "CI drift guard for gen:tokens: regenerate and fail if the committed tokens.css differs — fix by running gen:tokens and committing (never edit tokens.css directly)",
    "gen:large-image": "Regenerate static/large-image.png by replaying large-image.svg onto the live canvas (Playwright)",
    "gen:style-covers": "Regenerate the AI style thumbnails in static/styles/ from source.svg via Gemini (needs GEMINI_API_KEY; -- --style X --temperature N for one style / different takes)",
    "gen:coloring-chalk": "Generate a page's CHALK outline (dedicated dark-mode line art, stored ink-on-white as .chalk.webp) from its pen outline via Gemini — solid-white eye whites/catchlights, gated on registration + new-ink checks; candidates land in .coloring-samples-dark/chalk/ (needs GEMINI_API_KEY; -- page-or-category… [--apply] [--max-attempts N] [-t F] [--notes \"…\"] [--force]; --rescore re-gates saved candidates offline)",
    "gen:coloring-fills": "Generate gated flat-color LIGHT fill candidates via Gemini into .coloring-samples/; --apply ships only when every requested page passes, otherwise committed raw/punched assets stay unchanged (needs GEMINI_API_KEY; -- page-or-category [--apply] [--samples N] [--temperature N])",
    "gen:coloring-fills:audit": "Audit the committed raw fills (tools/asset-gen/fill-src/) for outline drift vs their line art (global + worst-tile keep); flags any that need regenerating. No API key/network (-- page-or-category, --overlay to dump drift maps)",
    "gen:coloring-fills:audit:eyes": "Audit the committed raw fills' EYES — flags a fill whose outlined eye rings were flooded one flat color, plus an `orb` column that scores the WHOLE pupil on the chalk-over-night composite and flags blank white orbs (chalk sclera + fill catchlight stacked over a solid-pen pupil — the band-blind class judgeNightEyes misses). Deterministic, no API key/network (-- page-or-category… to filter)",
    "gen:coloring-fills:audit:eyes:review": "Build a self-contained HTML review of every BLANK-ORB-flagged night eye (the `orb` column of gen:coloring-fills:audit:eyes) — whole-page composite + zoomed eye crops with median/white — to publish with the Artifact tool and triage real defects vs over-flags before burning them down. Deterministic, no API key/network (-- page-or-category…, --out FILE)",
    "gen:coloring-fills:audit:shapes": "Audit the committed raw fills for INVENTED COLORED SHAPES floating on the open background (extra stars/flowers no drift/white/eye gate can see; anchoring to source lines or the border is the discriminator). Deterministic, no API key/network (-- page-or-category…[.light|.night], --verbose, --overlay to dump detection maps)",
    "gen:coloring-fills:audit:halo": "Rank every shipped night fill by RESIDUAL DARK HALO around the chalk strokes after the punch (re-inked outlines/drop-shadows the lineWhite gate misses); top of the table needs human crop review. Deterministic, no API key/network (-- page-or-category… to filter, --out FILE for per-page JSON to diff across punch changes)",
    "gen:coloring-outlines:audit": "Audit shipped line art for SOLID black regions (they invert to white blobs in dark mode; ADR pending); lists offenders for gen:coloring-outlines:normalize. Deterministic, no API key/network (-- page-or-category… to filter)",
    "gen:coloring-outlines:fresh": "Author a BRAND-NEW pen outline for a page from a text scene description (no conditioning on the old drawing) via Gemini, gated on solidity/ring-depth/eye-cores/border; candidates land in .coloring-samples/fresh/ (needs GEMINI_API_KEY; -- page --scene \"…\" [--eyes] [--apply] [--max-attempts N] [-t F] [--notes \"…\"])",
    "gen:coloring-outlines:normalize": "Redraw an outline's solid black regions as thin outlined strokes via Gemini, gated on solidity + outline registration; candidates land in .coloring-samples-dark/normalize/ (needs GEMINI_API_KEY; -- page… [--apply] [--max-attempts N] [-t F] [--notes \"…\"])",
    "gen:coloring-golden:freeze": "Freeze every offline audit score for the whole coloring catalog (solidity/eye rings, light keep/localKeep + eyes, night drift/bgLuma/lineWhite + eyes/blank-orbs) into tools/asset-gen/golden/golden-scores.json — the committed regression baseline. Deterministic, no API key/network (~1 min)",
    "gen:coloring-golden:diff": "Re-run every offline audit and diff against golden/golden-scores.json: verdict flips (including composite blank-orbs) and bad-direction score movements = regressions (exit 1); improvements/moves reported. Run after any pipeline or asset change; re-freeze to adopt intended changes. No API key/network (~1 min)",
    "gen:coloring-punch": "Re-derive the shipped fills-only .light/.night.webp from the raw fills in tools/asset-gen/fill-src/ by punching their outlines out (line art as mask). Deterministic, no API key/network (-- page-or-category… to filter)",
    "gen:coloring-thumbs": "Regenerate the .thumb.webp grid thumbnails beside every coloring cover/page for the picker (-- category… to filter)",
    "gen:coloring-book-proof-sheet": "Build a self-contained HTML proof sheet of ONE coloring category — line art, chalk, light + night fills side by side with outline-keep scores, plus the composited view — to review/publish as an Artifact (-- category[/page[-orient]], --source shipped|samples, --out FILE)",
    "gen:icons-sheet": "Build the icon-gallery scrapbook page (ADR-0059): every icon shipped in web/src/lib/icons/ rendered at size and split into colorful spot illustrations vs monochrome UI glyphs (by chroma). Self-contained HTML; publish with scrapbook:publish. Deterministic, no network (-- --out FILE; defaults to a gitignored scratch dir)",
    "gen:e2e-tuning-report": "Rebuild the E2E tuning scrapbook page (ADR-0059) from the worker-count measurements recorded in the script: the sweep tables, the local/CI comparison, and the flake hypotheses that were falsified. Edit the datasets at the top of scripts/gen-e2e-tuning-report.mjs after a re-tune, then re-run. Deterministic, no network",
    "gen:assets:manifest": "Rewrite tools/asset-gen/golden/asset-manifest.sha256 (sha256 per committed art asset: coloring pages, style covers, fill-src raws) so binary asset changes review as text diffs; verified in CI by check:assets:manifest",
    "gen:shots": "Regenerate the store screenshots + feature graphic for both stores into store-assets/ (Playwright)",
    "scrapbook:publish": "Promote a keeper run output into the committed scrapbook/ tree and rebuild the index — copies a file or dir under scrapbook/<type>/<name> for a live GitHub Pages URL (ADR-0059). Usage: -- <source> <type>/<name>, e.g. -- lighthouse-reports lighthouse/latest",
    "scrapbook:index": "Rebuild scrapbook/index.html and the coloring-book proof-sheet hub from the current scrapbook/ tree — no copy",
    "scrapbook:proof-sheet-hub": "Rebuild scrapbook/coloring-book-proof-sheets/index.html from its shared chrome and category registry",
    "scrapbook:check": "Drift guard (CI): fail if any scrapbook/ collection dir has no reachable entry page, so the index's collection count always matches the cards it renders (md-only collections included)",
    "cap:sync": "build:cap + copy web assets & plugins into the native projects",
    "cap:android": "cap:sync + open the Android project in Android Studio",
    "cap:ios": "cap:sync + open the iOS project in Xcode (macOS-only)",
    "android:setup": "One-time emulator setup: installs the API 33 system image and creates the Pixel_7_Pro_API_33 AVD",
    "android:boot": "Boot the Pixel_7_Pro_API_33 emulator",
    "android:emulator": "cap:sync + build, install, and launch on the Pixel_7_Pro_API_33 emulator",
    "android:live": "Run on the emulator with live reload against the dev server on port 5173",
    "android:apk": "Debug APK → android/app/build/outputs/apk/debug/",
    "android:run": "Build + install the debug app onto the connected device/emulator",
    "android:run:device": "cap:sync + build, install onto the physical 'SM-S938U1' phone (ANDROID_SERIAL=R5CY128YMGF; use when an emulator is also connected — find your serial with adb:devices)",
    "android:bundle": "Signed release AAB; needs android/keystore.properties",
    "android:verify": "Verify the release AAB's signature (expect 'jar verified')",
    "android:open": "Reveal the release bundle folder in the OS file manager",
    "android:clean": "Gradle clean, no cap:sync",
    "ios:build": "Debug build for the iOS Simulator → ios/App/build/Build/Products/Debug-iphonesimulator/App.app (macOS-only)",
    "ios:run": "cap:sync + build, install, and launch, prompting to choose the iOS simulator or connected device (macOS-only)",
    "ios:run:emulator": "cap:sync + build, install, and launch on the iPad mini (A17 Pro) simulator (macOS-only; use ios:run to pick a device)",
    "ios:run:device": "cap:sync + build, install, and launch on the physical 'Kyle’s iPad' device (macOS-only; needs signing + a trusted developer cert — see the mobile guide)",
    "ios:live": "Run on a simulator/device with live reload against the dev server on port 5173 (macOS-only; pair with dev:cap)",
    "ios:archive": "Release archive (signed; needs the Apple Developer team set in Xcode) → ios/App/build/App.xcarchive (macOS-only)",
    "ios:ipa": "ios:archive + export the App Store .ipa → ios/App/build/ipa/ (macOS-only; see the mobile guide)",
    "ios:open": "Reveal the exported .ipa folder in the OS file manager",
    "ios:clean": "Xcode clean of the App scheme (macOS-only)",
    "adb:devices": "List connected Android devices (expect 'device', not 'unauthorized')",
    "adb:reverse": "Forward the phone's port 5173 to the desktop dev server (re-run after each USB reconnect)",
    "deps:gha": "Report the GitHub Actions version pins across .github/workflows and flag drift; add --check-latest to compare each against its latest upstream release (network)",
    "audit:preflight": "Read-only go/no-go check before an unattended audit burndown: selected agent auth, clean tree, origin reachable, docs/AUDIT.md parses, npm run check green (burn-down-audits skill)",
    "audit:burndown": "Scripted burndown of docs/AUDIT.md: one isolated Claude/Codex session per role per finding (verify → implement → adversarial review → fix), one commit each, pushed every finding. MAX_ISSUES=5 canary by default; knobs are env vars — see the burn-down-audits skill",
    "audit:burndown:overnight": "Launch audit:burndown unattended (-- N findings, default 600): preflight-gated, spawned detached so a closed terminal can't kill it. Stop with `touch .audit-work/STOP`",
    "audit:status": "Where the audit burndown stands: completed/deferred/remaining counts, progress bar, run state, unposted PR comments, recent Audit: commits, last log lines",
    "audit:cost": "Audit-burndown usage from saved Claude JSON or Codex JSONL: cost/tokens by role, per-issue average, projected remaining total",
    "audit:watch": "Live view of a running audit burndown: tail -f the run log, or -- --dash for a status summary refreshing every 10s",
    "prerelease": "check:assets + check:assets:manifest gates before release",
    "release": "Cut a release from releases/<version>.md: bump versions, commit, tag, push (use the /release command to draft notes first). Attaches no store artifacts — they cannot exist until this bump is committed",
    "release:publish": "Attach the built .aab/.ipa to an existing GitHub Release, refusing any artifact whose embedded version does not match (run after /release then /build)"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@capacitor/android": "^8.4.1",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@dprint/json": "^0.23.0",
    "@dprint/markdown": "^0.22.1",
    "@dprint/typescript": "^0.96.1",
    "@eslint/js": "^10.0.1",
    "@intellectronica/ruler": "0.3.44",
    "@playwright/test": "^1.61.1",
    "dprint": "^0.55.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-svelte": "^3.19.0",
    "globals": "^17.7.0",
    "happy-dom": "^20.10.6",
    "knip": "^6.29.0",
    "prettier": "^3.8.4",
    "prettier-plugin-svelte": "^4.1.1",
    "scripts-info": "^1.0.5",
    "sharp": "^0.35.2",
    "svelte-check": "^4.7.4",
    "svgo": "^4.0.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0",
    "vitest": "^4.1.10",
    "ws": "^8.21.1"
  },
  "dependencies": {
    "@aparajita/capacitor-secure-storage": "^8.0.0",
    "@capacitor-community/media": "^9.1.0",
    "@capacitor/core": "^8.4.2",
    "@capacitor/device": "^8.0.3",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/network": "^8.0.1",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/screen-orientation": "^8.0.1",
    "@capacitor/status-bar": "^8.0.3",
    "@fontsource-variable/quicksand": "^5.3.0",
    "@google/genai": "^2.10.0",
    "@netlify/blobs": "^10.7.9",
    "@sveltejs/adapter-netlify": "^6.0.4",
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.70.1",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "idb": "^8.0.3",
    "marked": "^18.0.5",
    "svelte": "^5.56.4",
    "vite": "^8.1.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "overrides": {
    "@capacitor/assets": {
      "sharp": "$sharp"
    },
    "tar": "^7.5.19"
  }
}

interface:
  display_name: 'Run Rival Agent'
  short_description: 'Run the rival agent: a fresh local Claude task, safely'
  default_prompt: 'Use $run-rival-agent to run this task through a fresh local Claude Code process and return its result.'

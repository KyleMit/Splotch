<!--
First line of the body: the issue this closes, e.g. `Fixes #NNN.` (or `Closes #NNN.`). Drop the line
if there is no issue.

Then a short lead paragraph — what this changes and why it needed changing — before the headings
below.

These headings are the floor, not the ceiling. Add your own for the parts of the change that need
explaining: the mechanism, what was measured, what was rejected, what is deliberately out of scope,
which ADRs or docs moved. Delete a section that genuinely does not apply.

Escape any `#`-number that is not a deliberate issue reference (`\#1`, or wrap it in backticks) —
GitHub turns a bare one into a link to an unrelated issue. Leave commit SHAs bare so they link.
-->

## What changed

## Verification

| check | result |
| ----- | ------ |
|       |        |

## Screenshots

<!--
Splotch is a visual app, so a UI change is not reviewable from the diff alone. Capture the running
app: before/after for a change, every state for a multi-state component, a gif or video for
animation. See the `pr-screenshots` skill for the conventions and for how to host the images.

If the change has no visible surface, say so here rather than deleting the section.
-->

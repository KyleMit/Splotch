export const VERSION_JSON_FILENAME = 'version.json';
export const VERSION_JSON_PATH = `/${VERSION_JSON_FILENAME}`;

// The query parameter the stale-page recovery navigation carries, naming the
// deployed version it is trying to reach.
export const CACHE_BUST_VERSION_PARAM = 'v';
